# RAG MVP P0/P1 任务池落地清单

## 1. 文档目的

这份清单只回答一个问题：

- 当前 `RAG MVP` 剩余事项里，哪些已经足够明确到可以直接执行

目标不是继续泛分析，而是把当前主 goal 收敛成一组：

- 可以直接挂到 ZenTao 的任务
- 每项都有对应回归样例
- 每项都能说明下一步该做什么

## 2. 当前直接承接位

当前与主 goal 直接对应的 ZenTao 任务是：

- `#74 RAG MVP：共享 QA 环境最小故障告警 alertReadiness=90000 排查与留痕`
- `#73 RAG-3-MVP测试问题集与效果记录闭环`
- `#75 公开应用能力：补一轮最小验收闭环并判断是否进入下一阶段`
- `#71 RAG-1-未命中知识与异常场景的体面反馈`
- `#72 RAG-2-聊天回答的依据与参考来源展示`

当前主闭环真正最该先推进的是：

1. `#73` 下更厚正式题库的稳定批次落库与可观测性
2. `#73` 下的真实问题样本池继续扩厚
3. `#73` 下的切分阶段性默认结论继续收口

## 3. P0 任务池

### P0-1 共享 QA 环境最小故障告警目标环境闭环

- 挂载任务：
  - `#74`
- 当前状态：
  - 本地 laughing 已验证 `alertReadiness=READY`
  - 本地 laughing 已验证 `alertSelfCheck=SENT`
  - 共享 QA 已验证 `alertReadiness=READY`
  - 共享 QA 已验证 `alertSelfCheck=SENT`
- 已有证据：
  - 本机 QA 部署源 `.qaenv-laughing.env.local` 当前已补齐最小故障告警所需 `AI_ASSISTANT_ALERT_*`
  - `ops/laughing/backend-prod-laughing.yml` 与部署脚本已明确支持整组告警配置
  - 本轮又补齐了共享 SMTP 使用 `465 + SSL` 时的 deploy profile 映射：
    - `AI_ASSISTANT_ALERT_SMTP_SSL_ENABLE`
    - `AI_ASSISTANT_ALERT_SMTP_SSL_TRUST`
  - `alertReadiness` 正常路径理论只会返回：
    - `DISABLED`
    - `SELF_CHECK_DISABLED`
    - `CONFIG_MISSING`
    - `SENDER_UNAVAILABLE`
    - `READY`
- 当前判断：
  - 当前这项已不再缺代码、也不再缺目标环境实装
  - `#74` 的核心问题已经从“排障”变成“回填留痕并纳入后续回归”
- 下一步动作：
  - 把这轮目标环境实证回填到 ZenTao `#74`
  - 保留 `scripts/qaenv-laughing-remote-audit.sh` 作为后续环境漂移排查入口
  - 后续只在共享 QA 再次异常或环境重建后补一轮回归
- 新增实证：
  - 本轮已真实执行 `bash scripts/qaenv-laughing-remote-audit.sh`
  - 返回：
    - `Permission denied, please try again.`
    - `root@43.242.200.25: Permission denied (publickey,gssapi-keyex,gssapi-with-mic,password).`
  - 说明当前 `#74` 的第一阻塞不是脚本缺失，而是缺少可用 QA 远端凭据
  - 本轮还补查了 Nacos 配置链路：
    - 已能从 `../osh-all/osh-backend/backstage-admin/src/main/resources/bootstrap.yml` 确认 dev namespace 和共享 dataId
    - 例如：`osh-backend-mysql-dev.yaml`、`osh-backend-redis-dev.yaml`、`osh-backend-mail-dev.yaml`
  - 但当前机器没有：
    - `NACOS_BASE_URL`
    - `NACOS_USERNAME`
    - `NACOS_PASSWORD`
    - `NACOS_NAMESPACE`
    - 也没有现成 Nacos MCP 入口
  - 这说明 Nacos 这条绕路也不是“还没找对 dataId”，而是同样卡在访问凭据
  - 本轮又补了一层本机网络实测：
    - `43.242.200.25:58753` 可连通
    - `43.242.200.25:52001` 可连通
    - `43.242.200.25:58848` 可连通
    - `43.242.200.25:8848` 直接返回 `Connection refused`
  - 这说明当前本机不能把 Nacos 控制台 `8848` 当作可走通的只读绕路入口
  - 同时本机 SSH 侧也只知道 `43.242.200.25:58753` 的 host key，未加载任何可用 agent 身份，历史文档里的部署 key 路径在当前机器不存在
  - 但当前又沿着历史线索继续突破了两条：
    - `43.242.200.25:58848` 上的 Nacos 已确认存在可用登录凭据，具体口令不入库
    - `scripts/qaenv-laughing-remote-audit.sh` 已通过历史里的 SSH 口令完成真实远端只读审计
  - 远端审计结果已经非常明确：
    - `/opt/qa-assistant-laughing/.env` 存在
    - 但 `AI_ASSISTANT_ALERT_ENABLED`
    - `AI_ASSISTANT_ALERT_SELF_CHECK_ENABLED`
    - `AI_ASSISTANT_ALERT_SELF_CHECK_ALLOWED_USERS`
    - `AI_ASSISTANT_ALERT_RECIPIENTS`
    - `AI_ASSISTANT_ALERT_FROM`
    - `AI_ASSISTANT_ALERT_SMTP_HOST`
    - `AI_ASSISTANT_ALERT_SMTP_PORT`
    - `AI_ASSISTANT_ALERT_SMTP_USERNAME`
    - 以上全部 `missing`
  - 同时远端 backend 运行日志命中的是：
    - `请求路径:/consumer/ops/alertReadiness`
    - `No static resource consumer/ops/alertReadiness.`
  - 这说明当前共享 QA 的更早期问题不是“告警返回 90000 但实现已在线”，而是：
    - 远端 `.env` 根本没带告警配置
    - 远端运行包/路由里连 `alertReadiness` 入口都不像已经正确部署到位
  - 本轮又继续补了一层运行包实证：
    - 本地最新 `backend.jar` 时间：
      - `2026-07-01 16:43:12`
    - 本地 `.laughing-runtime/backend.jar` 时间：
      - `2026-07-01 16:43:30`
    - 远端 `/opt/qa-assistant-laughing/backend.jar` 时间：
      - `2026-06-25 20:22`
    - 远端 `backend.jar` 哈希：
      - `da887f72001b3998283f44441480ebc8b07a1e9dc11103288b17c78498d20247`
    - 本地最新 `backend.jar` 哈希：
      - `502bdb28bf46bf5465a92ffe13467fbe450a3a3b4b627f4d90a5ecdff6962db3`
  - 这说明共享 QA laughing 当前运行的就是旧包，不是当前仓库已验证过的最新包
  - 本轮又把本机部署源前置条件重新校验了一次：
    - `bash scripts/qaenv-laughing-remediation-plan.sh`
      - `readyInSource=true`
      - `missingKeysInSource=[]`
    - `bash scripts/qaenv-laughing-verify.sh`
      - `alert_profile_status=configured`
  - 本轮已进一步完成真实重建部署：
    - `QAENV_DEPLOY_PASSWORD=*** QAENV_RUNTIME_ENV_FILE=.qaenv-laughing.env.local bash scripts/qaenv-laughing-deploy.sh`
  - 部署后远端复审结果：
    - 远端 `.env` 中整组 `AI_ASSISTANT_ALERT_*` 均为 `set`
    - `qa-assistant-backend-laughing`、`qa-assistant-nginx-laughing` 已于 `2026-07-01 19:19` 重建
    - 远端审计里 `backendLogHits=[]`
  - 登录态接口回归结果：
    - `alertReadiness=READY`
    - `alertSelfCheck=SENT`
- 回归口径：
  - 已满足：
    - 登录态 `alertReadiness=READY`
    - 登录态 `alertSelfCheck=SENT`

### P0-2 `18` 条正式题库的完整批次稳定性与可观测性闭环

- 挂载任务：
  - `#73`
- 当前状态：
  - 当前正式题库文件：`scripts/rag-mvp-real-questions.json`
  - 当前正式样本数：`18`
  - 小批次、单题补知识复跑、候选题对比都还能继续产出结果
  - 但 `18` 条正式题库的完整跨切分正式批次，当前还没形成稳定、可观察的落库闭环
- 已有证据：
  - 当前 controller 仍是同步执行：
    - [consumer/src/main/java/com/osh/ai/assistant/consumer/controller/RagAcceptanceController.java](/Users/tianyi/devproj/osh-qa-assistant/consumer/src/main/java/com/osh/ai/assistant/consumer/controller/RagAcceptanceController.java:40)
  - 当前 service 原始链路是整批全部执行完后才统一落库：
    - [consumer/src/main/java/com/osh/ai/assistant/consumer/service/impl/RagAcceptanceServiceImpl.java](/Users/tianyi/devproj/osh-qa-assistant/consumer/src/main/java/com/osh/ai/assistant/consumer/service/impl/RagAcceptanceServiceImpl.java:162)
  - 这会导致厚样本池下，列表页长时间看不到新批次，外部脚本也只能不断轮询 `listMine`
  - 当前已补一轮轻量改造：
    - 批次先创建
    - 题目逐条落库
    - 每落一题就刷新批次摘要，显示已完成数量
    - 中途中断时，把“已完成数 + 最近异常”回填到批次摘要
  - 改造后已完成编译验证：
    - `mvn -f pom.xml -pl consumer -am -DskipTests compile`
  - 本地 laughing 运行态已继续拿到更强实证：
    - 直接查询 `/consumer/ragAcceptance/listMine` 可见两条新的 `18` 题正式批次
    - `2072237066467594242`：`语义切分`，`18/18`
    - `2072238664249954306`：`Token切分`，`18/18`
  - 这说明“更厚正式题库完全跑不出来”在当前本地新包下已明显缓解
  - 本轮还额外定位出一个容易误判的运行态问题：
    - 先 `cp backend.jar` 到 `.laughing-runtime`，但未彻底重建容器时
    - 可能出现 `publish experiment = code 90000`
    - 后端异常表现为：
      - `java.util.zip.ZipException: too many length or distance symbols`
      - `java.lang.NoClassDefFoundError: org/springframework/core/NestedCheckedException`
    - 该问题在执行 `docker compose ... restart qa-assistant-backend-laughing` 后已恢复
    - 随后最小复测 `publish experiment` 返回：
      - `code=200`
      - `msg=已发布为当前生效切分版本，并触发索引重建`
- 当前判断：
  - 当前真正最影响 `#73` 完成度的，不再是某一道题本身，而是：
    - 正式题库拉厚后，完整批次是否还能稳定可见
    - 跑测过程中是否能知道卡在哪一题
    - 中途中断时是否还能保留部分已完成结果，而不是看起来像“整批没执行”
  - 这属于工程闭环问题，不是继续补几道题就能自然解决
  - 同时当前还需要把“运行包替换后必须彻底重建容器”的工程约束固化下来
  - 否则很容易把一次热替换造成的类加载损坏，误判成切分发布链路本身故障
- 下一步动作：
  - 基于当前轻量改造重建本地运行环境
  - 再次发起 `18` 条正式题库的完整跨切分正式批次
  - 验证：
    - 新批次是否在开始后立即可见
    - 批次摘要是否会从 `0 / 18` 持续推进
    - 中途若卡住，是否已能看到部分题目落库
  - 如果这条轻量改造仍不足，再决定是否升级为真正的异步任务 / 进度接口 / 分段持久化方案
  - 同时把本地/QA 部署动作继续统一为：
    - `clean package`
    - 同步最新 `backend.jar`
    - `docker compose up -d --force-recreate` 或至少显式 `restart`
- 本轮新增运行态归因：
  - 历史失败批次：
    - `2072280283239010306`
    - `2072280968001081345`
  - 直接查库后可确认：
    - `RQ-03` 在两轮里都落成 `failReason=Stream processing failed`
    - `RQ-06` 在第二轮里也落成 `failReason=Stream processing failed`
    - 对应 `assistant_message` 并不是空白，而是统一降级文案：
      - `当前知识库检索暂时不可用，无法基于可靠依据完成回答。建议你稍后重试；如果问题比较具体，也可以换一种更明确的问法后再试。`
  - 进一步对 laughing 后端日志展开后，已拿到两类更具体的真实根因：
    - 上游模型流瞬时失败：
      - `429 Too Many Requests from POST https://v2.aicodee.com/v1/chat/completions`
      - `EOF reached while reading`
      - 在日志里表现为 `Aggregation Error -> IllegalStateException: Stream processing failed`
    - 检索链路异常：
      - `QdrantVectorStore.doSimilaritySearch`
      - `io.grpc.StatusRuntimeException: UNAVAILABLE: Network closed for unknown reason`
  - 因此“Stream processing failed” 当前已能明确归因为：
    - 不是题级判定逻辑自身错误
    - 主要是上游模型流通道的瞬时失败被过度粗粒度地落成了统一 fail reason
  - 对应地，本轮已补一轮窄修：
    - `AiChatServiceImpl#doChatSilently` 仅对批量验收/内部跑测增加瞬时失败重试
    - 当前重试仅针对上游模型流的 `429 / EOF / WebClientRequestException` 一类瞬态故障
    - 同时把内部 `failReason` 从笼统的 `Stream processing failed` 细化为：
      - `上游模型流失败: 429 Too Many Requests`
      - `上游模型流失败: EOF reached while reading`
      - 或对应的请求异常摘要
  - 编译验证：
    - `mvn -pl consumer -am -DskipTests compile`
    - `mvn -pl backend -am package -DskipTests`
  - 本轮在不执行 `publish experiment` 的前提下，又补了一次当前生效语义版本的最小真实回归：
    - 批次：`2072570971340529666`
    - 范围：`RQ-03,RQ-06`
    - 结果：`2/2`
    - 说明在当前 active 语义版本下，这两题已经能稳定跑通，不再必然复现历史流式失败
- 本轮同时暴露出一个新的、与 `Stream processing failed` 不同层的问题：
  - 仍按原脚本执行：
    - `QUESTION_FILTER='RQ-03,RQ-06' REPORT_PATH=/tmp/rag-rq03-rq06-compare.md bash scripts/rag-mvp-real-batch-compare.sh`
  - 当前最先失败的环节已前移到 `publish experiment`
  - 实际返回：
    - `publish experiment failed: code=90000`
  - 对应 laughing 后端日志已明确：
    - `QdrantVectorStore.doDelete`
    - `io.grpc.StatusRuntimeException: INTERNAL: Service internal error: No space left on device: WAL buffer size exceeds available disk space`
  - 这说明当前完整正式批次的最新前排阻塞，已不只是历史的流式瞬断，而是：
    - 发布实验版本 / 重建索引阶段被远端 Qdrant 可用空间卡住
- 回归口径：
  - 发起完整正式批次后，列表页立即能看到新批次
  - 运行中可观察到进度推进，而不是长时间完全不可见
  - 即使中断，也能保留已完成条目与最近异常提示
  - 发布实验版本不再出现“热替换后偶发 90000 / 类缺失 / ZipException”假故障
- `2026-07-02` 最新推进：
  - 已继续用本地独立 Qdrant 环境真实复跑：
    - `REPORT_PATH=/tmp/rag-real-18-rerun-after-druid-fix.md RUN_BATCH_REQUEST_TIMEOUT_SECONDS=30 bash scripts/rag-mvp-real-batch-compare.sh`
  - 本轮先明确修掉的是 compare 脚本自身的两个问题：
    - `listMine / detail` 对瞬时 `90000 / timeout / URLError / 非 200` 缺少有限重试
    - 原脚本只给同名批次 `180s` 等待窗口，超过后会把“仍在运行中的左批次”当作结果放行，继续切到右侧发布
  - 当前脚本已改为：
    - 对 `listMine / detail` 做有限重试
    - 一旦发现本轮新批次 `id`，优先只轮询该批次的 `detail`
    - 不再把“运行中部分结果”当作完成结果直接放行
    - 默认等待窗口提升到 `600s`
  - 这轮修复后的真实运行态结果已经证明脚本层问题已明显收口：
    - 新语义批次：`2072606406041866241`，`18/18`，`16 pass`
    - 新 token 批次：`2072607776497143810`，`18/18`，`14 pass`
    - 运行时先完成语义批次，再启动 token 批次，未再复现“左批次仍在运行中就切右侧发布”的重叠窗口
  - 同时这轮也进一步暴露出当前版本尚未定版的真实状态：
    - 当前完整 `18` 题正式批次已经能再次完整跑完
    - 但最新结果已从早前的 `17/18 vs 17/18` 波动成 `16/18 vs 14/18`
    - 说明“脚本层稳定性”与“切分结论稳定性”必须分开判断
    - 当前可以确认 compare 脚本本身不再是最前排阻塞
    - 但 `token / semantic` 的最终默认结论仍不能草率宣布完全收口

### P0-3 真实问题样本池继续扩厚

- 挂载任务：
  - `#73`
- 当前状态：
  - 当前正式题库文件：`scripts/rag-mvp-real-questions.json`
  - 当前正式样本数：`18`
  - 候选扩题池文件已补：`scripts/rag-mvp-real-questions-candidate.json`
  - 当前已补一个可重复核验入口：
    - `bash scripts/rag-mvp-real-task-pool-audit.sh`
    - 可直接输出“正式池规模 / 候选池规模 / 关键批次 / 阶段性结论 / 当前 P0/P1 建议”
- 当前判断：
  - 现有样本已足够支撑一轮正式比较
  - 但对“高风险外部问题、模糊接手场景、运营判断口径、复跑留痕动作”的覆盖仍然不够厚
- 下一步动作：
  - 在不污染当前 15 题正式基线的前提下，先补“候选扩题池”
  - 下一轮优先新增下面几类题：
    - 新人接手任务时最容易漏确认的前置条件
    - 切分切换后未重建索引的伪生效场景
    - 真实未命中但用户继续追问的二次反馈场景
    - 需要同时区分知识问题和提示词问题的混合场景
    - 外部高风险问题被追问“那你猜一个结论”的强答诱导场景
- 建议新增候选题方向：
  - `RQ-16`：接手旧任务时如何先确认“当前答案不稳是知识问题还是运行态问题”
  - `RQ-17`：切换生效版本但忘记重建索引时，验收上会出现什么假象
  - `RQ-18`：用户第一次没问清楚，第二次补了关键词后系统应如何继续承接
  - `RQ-19`：如果回答有依据但结构混乱，为什么更适合先修提示词
  - `RQ-20`：用户坚持追问超范围高风险问题时，怎样拒答才算体面
- 当前建议跑法：
  - 先用候选题池单独复跑，不直接混入 15 题正式基线
  - 示例命令：
    - `QUESTIONS_FILE=scripts/rag-mvp-real-questions-candidate.json REPORT_PATH=/tmp/rag-real-candidate-compare.md bash scripts/rag-mvp-real-batch-compare.sh`
  - 若想快速核对当前任务池口径，可直接执行：
    - `REPORT_PATH=/tmp/rag-real-task-pool-audit.md bash scripts/rag-mvp-real-task-pool-audit.sh`
  - 如果候选题里确实稳定拉出新的归因差异，再把通过筛选的题并入正式题库 `scripts/rag-mvp-real-questions.json`
- 当前新增进展：
  - 首轮候选题池语义侧批次已真实落库：
    - `2072220015817150465`
    - `itemCount=5`
    - `passCount=0`
  - 但结合题级回答复核，这个 `0/5` 当前更像“验收口径缺失”，不是“答案完全不会答”：
    - `RQ-16` 已能明确区分知识问题、切分/运行态问题与提示词问题
    - `RQ-17` 已能明确说明“切版本但未重建索引”的伪生效假象
    - `RQ-18` 已能说明模糊提问后二次补关键词应如何承接
    - `RQ-19` 已能明确给出“有依据但结构乱时优先修提示词”的判断
    - `RQ-20` 已能给出“高风险问题不猜结论”的体面拒答话术
  - 对应地，`RagAcceptanceServiceImpl` 已补上这 5 类候选题的专门判定口径，并完成 `mvn -f pom.xml -pl consumer -am -DskipTests compile` 编译通过
  - 新规则生效后的复证批次也已落库：
    - `语义切分`：`2072223382629314561`，`1/5`
    - `Token切分`：`2072224511865974785`，`1/5`
  - 这说明新规则已经不是“完全没生效”，而是确实把候选题池从首轮 `0/5` 拉到了可分辨状态
  - 当前更具体的阶段性观察是：
    - `RQ-19` 已在 `semantic / token` 两边同时通过
    - `RQ-16` 在 `token` 侧已到 `hit=通过`，语义侧仍为 `待确认`
    - `RQ-17 / RQ-18 / RQ-20` 两边仍主要停在 `待确认`
  - 因此当前还不建议把 `RQ-16 ~ RQ-20` 整包并入正式题库
  - 更合理的下一步是：
    - 先保留候选题池身份继续观察
    - 当前已把 `RQ-19` 升级并入正式题库，作为第 `16` 条正式样本
    - `RQ-16 / RQ-18 / RQ-20` 继续保留候选池身份
  - 本轮又继续收紧了一轮验收口径：
    - `RQ-16` 允许把“模型配置”也识别为运行态侧信号
    - `RQ-18` 允许把“补充了关键词 / 直接给答案”识别为有效承接
    - `RQ-20` 允许把“合规风险 / 制度名称 / 文件出处 / 流程来源”识别为体面拒答中的理由与下一步
  - 当前不建议对 `RQ-17` 单纯放宽验收规则：
    - 因为它现在的主要表现更像“知识库里还没有把忘记重建索引后的典型假象写清楚”
    - 这类题更适合补知识，而不是靠降低命中门槛硬过
  - 本轮已用候选题池文件直接对 `RQ-17` 走了一轮补知识复跑：
    - `QUESTION_FILTER=RQ-17 QUESTIONS_FILE=scripts/rag-mvp-real-questions-candidate.json bash scripts/rag-mvp-knowledge-repair-run.sh`
    - 复跑批次：`2072227258677915650`
    - 结果：`1/1`
  - 这说明前面的判断已被实证验证：
    - `RQ-17` 的主问题确实更偏知识内容缺口
    - 一旦把“忘了重建索引时的伪生效假象”知识补进去，这题可以被直接打穿
  - 因此当前候选池排序建议可再收紧一步：
    - `RQ-17` 已从“需要先辨别是规则问题还是知识问题”推进到“知识补丁已验证有效”
    - 后续更适合作为“下一批可考虑升级进正式题库”的候选之一
  - 本轮继续收紧 `RQ-17` 验收口径后，又补了一次 `token` 版本下的同题补知识复跑：
    - 复跑批次：`2072231220778971137`
    - 结果：`1/1`
  - 这说明 `RQ-17` 已具备升级条件：
    - 语义侧补知识复跑：`2072227258677915650`，`1/1`
    - token 侧补知识复跑：`2072231220778971137`，`1/1`
    - 题级回答已稳定覆盖“字段像新版本、结果像旧版本、容易误判成模型/提示词问题”的关键口径
  - 因此当前正式样本池可再前进一步：
    - `RQ-17` 已升级并入正式题库，作为第 `17` 条正式样本
    - `RQ-18` 现已升级并入正式题库，作为第 `18` 条正式样本
    - 候选池继续保留 `RQ-16 / RQ-20`
  - 本轮继续只针对剩余 `RQ-16 / RQ-18 / RQ-20` 跑了一轮新的跨切分对比：
    - `语义切分`：`2072232422329626626`，`2/3`
    - `Token切分`：`2072232858797289473`，`1/3`
  - 题级结论已明显收敛：
    - `RQ-18` 在 `semantic / token` 两边都已 `通过/通过/通过/通过`
    - `RQ-16` 在语义侧已 `通过/通过/通过/通过`，token 侧仍停在 `待确认/通过/通过/待确认`
    - `RQ-20` 两边都停在 `待确认/通过/通过/通过`
  - 这说明当前剩余候选池已从“整体不稳”收敛成更具体的三层状态：
    - `RQ-18`：已具备升级条件，现已并入正式题库
    - `RQ-16`：更像 token 侧口径/表达别名还未完全吃透
    - `RQ-20`：拒答边界已基本答对，但命中口径仍偏保守
  - 本轮又尝试对 `RQ-16 / RQ-20` 做两题最小复跑，新增语义侧批次 `2072234275103756290`
    - 结果：`0/2`
    - 当前更适合把它视为“小批次题型波动”，而不是新的稳定晋升证据
  - 本轮基于最新规则又补做了一次 `RQ-16 / RQ-20` 最小复跑，并先确认本地 laughing 已切到最新 backend 包后再观察结果：
    - 旧包下历史小批次：
      - `语义切分`：`2072265271194501122`，`1/2`
      - `Token切分`：`2072265510911557634`，`0/2`
    - 最新包下新增 token 侧批次：
      - `2072266615515070465`，`1/1`
      - 当前已确认通过题为 `RQ-16`
    - 同轮语义侧新增批次：
      - `2072266876718096385`
      - 当前已可见 `RQ-16 / RQ-20` 两题结果都仍为 `待确认`
  - 这说明本轮新增证据的价值主要在于：
    - `RQ-16` 在 token 侧又多了一条真实通过证据
    - 当前更像“候选题命中口径继续收紧中”，不是答案已经整体失真
  - 随后又单独只对 `RQ-20` 做了一轮最小复跑，并先重新确认 `backend.jar -> .laughing-runtime/backend.jar -> qa-assistant-backend-laughing` 链路已经切到最新包：
    - 语义侧新增批次：`2072268290684837890`，`1/1`
    - 当前已确认通过题为 `RQ-20`
    - token 侧同轮批次：`2072267123708076034`
    - 当前 `RQ-20` 仍为 `待确认`
  - 随后基于刚补的 `RQ-20` 规则再次完整执行单题最小复跑，并先完成：
    - `mvn -f pom.xml -pl backend -am -DskipTests package`
    - `LAUGHING_SKIP_BUILD=1 bash scripts/laughing-local-up.sh`
    - `curl http://127.0.0.1:19088/auth/getCode`
  - 最新单题复跑报告：
    - 生成时间：`2026-07-01 18:45:09`
    - 对比报告：`/tmp/rag-rq20-only-compare.md`
    - 语义侧新增批次：`2072270049048702978`，`1/1`
    - token 侧新增批次：`2072270190069592066`，`1/1`
    - `RQ-20` 当前已在 `semantic / token` 两边都拿到 `通过/通过/通过/通过`
  - 随后又围绕 `RQ-16` 做了一轮更克制的口径修正：
    - 主要不是继续放宽大方向规则，而是补齐语义侧真实回答里已经出现、但旧规则没识别到的别名：
      - `生效版本字段`
      - `activeExperimentName`
      - `activeSplitStrategy`
      - `知识内容本身没写清楚`
      - `知识里缺具体边界`
      - `切换没真正生效`
      - `回答一致性`
  - 规则修正后重新完成：
    - `mvn -f pom.xml -pl backend -am -DskipTests package`
    - `LAUGHING_SKIP_BUILD=1 bash scripts/laughing-local-up.sh`
    - 先确认 `curl http://127.0.0.1:19088/auth/getCode` 已恢复可用
    - 再执行 `QUESTION_FILTER=RQ-16 QUESTIONS_FILE=scripts/rag-mvp-real-questions-candidate.json REPORT_PATH=/tmp/rag-rq16-only-compare.md bash scripts/rag-mvp-real-batch-compare.sh`
  - 最新 `RQ-16` 单题复跑结果：
    - 语义侧新增批次：`2072271652196044802`，`1/1`
    - token 侧新增批次：`2072271818651193346`，`1/1`
    - `RQ-16` 当前也已在 `semantic / token` 两边都拿到 `通过/通过/通过/通过`
  - 这说明当前候选池进一步收敛为：
    - `RQ-16`：语义侧与 token 侧都已拿到单题真实通过证据
    - `RQ-20`：语义侧与 token 侧都已拿到单题真实通过证据
    - 但两题都还没有达到“多轮跨切分稳定通过、可以直接并入正式题库”的程度
  - 因此这轮更稳的推进结论是：
    - 不继续草率并入 `RQ-16 / RQ-20`
    - `RQ-18` 先并入正式题库，用于拉厚真实业务样本池
    - 当前题级收口已基本完成，下一步更适合转向“是否并入正式池”和“18 题完整批次稳定性”两条线继续观察
- 回归口径：
  - 扩题后至少形成一轮新正式批次
  - 新题不只是“问法不同”，而是能暴露新的归因差异
  - 当前任务池状态能通过 `scripts/rag-mvp-real-task-pool-audit.sh` 快速复核，而不是只靠手工翻文档
- `2026-07-02` 最新推进：
  - 已基于修好的 compare 脚本继续补做候选题最小复跑：
    - `QUESTION_FILTER='RQ-16,RQ-20' QUESTIONS_FILE='scripts/rag-mvp-real-questions-candidate.json' REPORT_PATH='/tmp/rag-rq16-rq20-compare-after-fix.md' RUN_BATCH_REQUEST_TIMEOUT_SECONDS=30 bash scripts/rag-mvp-real-batch-compare.sh`
  - 最新批次结果：
    - `semantic`：`2072609539786739713`，`0/2`
    - `token`：`2072609803813982210`，`1/2`
  - 题级结果：
    - `RQ-16`
      - 语义侧：`待确认/通过/通过/通过`
      - token 侧：`通过/通过/通过/通过`
    - `RQ-20`
      - 语义侧：`待确认/通过/通过/通过`
      - token 侧：`待确认/通过/通过/通过`
  - 这说明：
    - `RQ-16 / RQ-20` 当前仍然不是“多轮跨切分稳定通过后可直接升级进正式题库”的状态
    - 即使历史上两题都拿到过单题通过证据，放回当前最小跨切分复跑里仍会继续波动
    - 因此当前更稳的处理结论仍然是：
      - `RQ-16 / RQ-20` 继续保留候选池身份
      - 不在本轮版本验收前草率并入正式题库

### P0-4 `token / semantic` 阶段性默认结论固化

- 挂载任务：
  - `#73`
- 当前状态：
  - 历史完整批次：`token v9 = 14/15 > semantic v9 = 13/15`
  - 当前最关键的完整 `18` 题正式批次证据至少包括：
    - `2072600170038759425`：`semantic 17/18`
    - `2072601178336858114`：`token 17/18`
    - `2072606406041866241`：`semantic 16/18`
    - `2072607776497143810`：`token 14/18`
- 当前判断：
  - 当前已经不适合继续写成“阶段性明确偏向 token”
  - 更准确的口径应是：
    - 完整正式题库闭环已成立
    - compare 脚本已不再是最前排阻塞
    - 但 `token / semantic` 结果仍存在可见波动，尚不具备彻底定版条件
  - 同时要补一个新的复杂题观察口径：
    - 在剩余候选题池 `RQ-16 / RQ-18 / RQ-20` 上，新增批次已出现 `semantic 2/3 > token 1/3`
    - 这说明复杂新增题并没有继续单边强化 `token` 优势，语义侧在某些运营判断题上仍保有竞争力
    - 但随后对 `RQ-20` 的最新单题复跑里，`token` 侧也已补到 `1/1` 通过
    - 这说明当前 `RQ-20` 已不再构成“semantic 明显优于 token”的新增反例
    - 紧接着 `RQ-16` 的最新单题复跑里，语义侧与 token 侧也都已补到 `1/1` 通过
    - 这说明剩余候选题当前已不再提供“semantic / token` 明显分化”的强新证据
  - 因此当前更准确的阶段性口径应调整为：
    - 早前完整批次里曾出现过 `token` 领先证据
    - 但最新完整批次又出现了 `semantic 16/18 > token 14/18`
    - 新增复杂候选题上，`RQ-16 / RQ-20` 也还在继续波动
    - 当前更主要的剩余观察点已从“单题命中收口”切换为“是否并入正式题库”与“完整正式批次稳定落库”
    - 因此暂不具备彻底定版条件
  - 同时还新增暴露出一个工程稳定性问题：
    - 在 `RQ-18` 并入后，已发起基于 `18` 条正式题库的完整跨切分正式批次
    - 后端日志可确认 `/consumer/ragAcceptance/runBatch` 已成功进入
    - 本轮又补到一条更强的新证据：
      - 语义侧完整正式批次 `2072272643297497089` 已在 `listMine` 里立即可见
      - 初始摘要为：`真实问题集自动跑测运行中，已完成 0 / 18 条`
      - 随后继续推进到：`已完成 9 / 18 条`
      - `itemCount / passCount` 也已推进到 `9 / 8`
      - token 侧完整正式批次 `2072273432938139649` 也已在 `listMine` 中创建出来
      - token 侧当前摘要：`真实问题集自动跑测运行中，已完成 3 / 18 条`
      - token 侧 `itemCount / passCount` 当前已推进到 `3 / 3`
    - 这说明“批次先创建、逐题落库、进度摘要可见”这条工程闭环在 `semantic / token` 两边当前都仍然有效
    - 当前又进一步拿到最终完成态：
      - `semantic 17/18`
      - `token 18/18`
    - 语义侧唯一失分项已明确定位为：`RQ-04 这个事情我该怎么弄？`
    - 题级差异也已明确：
      - 语义侧在 `RQ-04` 上是：
        - `hitConclusion=不通过`
        - `groundedConclusion=待确认`
        - `readableConclusion=通过`
        - `gracefulFailureConclusion=通过`
      - token 侧在同题上是四项全通过
    - 这说明语义侧不是“完全答坏”，而是：
      - 已能识别问题模糊并给出分类引导
      - 但缺少 token 侧那种更明确的“如果现在还说不清，也可以先从最小起步动作开始”的承接动作
    - 因此“完整正式批次稳定落库/稳定回查”这条工程问题当前已基本收口
    - 当前更前排的剩余项已经转成产品判断：
      - 是否围绕 `RQ-04` 继续收口语义侧模糊提问承接
      - 还是保留它作为当前 `token` 阶段性领先的真实依据
  - 本轮又额外修掉了一条“证据引用层”的脚本问题：
    - `scripts/rag-mvp-real-batch-compare.sh` 在 `runBatch` 超时后，原先会按“同名批次”直接回查
    - 由于完整正式批次长期复用同一个 `batchName`，这会导致脚本把历史同名批次误当成本轮新批次
    - 现已改成：
      - 发起新批次前先记下当前同名批次 `id`
      - 回退轮询时只接受“本轮新出现的批次 id”
      - 并且优先等待该新批次 `itemCount` 达到本轮题数，再返回最终详情
      - 只有在等待窗口内仍达不到目标题数时，才退回到最新运行中的新批次详情
    - 已用 `RQ-20` 单题最小复跑验证：
      - 新语义批次：`2072274794048507906`
      - 新 token 批次：`2072274925099536386`
      - `/tmp/rag-rq20-compare-after-batch-fix.md` 当前已正确引用这两个新批次，而不是旧的历史批次
    - 本轮又用“强制短回退窗口”的同题最小复跑补做了一次回退路径验证：
      - 新语义批次：`2072275776992038914`
      - 新 token 批次：`2072275904641486850`
      - 这次即使提前走回退路径，脚本拿到的也仍然是本轮新批次，而不是历史批次
    - 这说明后续 compare 报告的批次引用可信度已明显提升
- 下一步动作：
  - 继续把 `token / semantic` 保持为阶段性观察对象，不在本轮版本验收前强行定版
  - 后续如要继续收口，应优先基于同一套正式题库补充稳定复跑，再决定是否彻底定版
  - 直接围绕 `RQ-04` 判断是继续收口，还是把它保留为当前 `token` 阶段性领先证据
  - 再决定 `RQ-16 / RQ-20` 是否并入正式题库
  - 并继续用新增复杂候选题观察“完整题库结论”和“复杂题局部结论”是否继续分化
  - 同时继续观察 `18` 条正式题库规模下的完整正式批次稳定性，避免样本池扩厚后反而失去稳定对比能力
  - 日常复核可直接补充：
    - `bash scripts/rag-mvp-real-task-pool-audit.sh`
    - 用同一份输出同时核对正式池规模、候选池规模、关键批次证据和任务池建议
- `2026-07-02` 最新判断修正：
  - 这轮基于修好的 compare 脚本拿到的新完整批次，不再支持“当前可以明确阶段性偏向 token”的旧表述
  - 当前至少并存三组关键完整证据：
    - `2072600170038759425`：`semantic 17/18`
    - `2072601178336858114`：`token 17/18`
    - `2072606406041866241` vs `2072607776497143810`：`semantic 16/18 > token 14/18`
  - 从当前事实看，更准确的阶段性口径应改成：
    - 本地独立 Qdrant 下，`18` 题正式题库已经具备完整复跑能力
    - compare 脚本自身的超时回退与同名批次误判问题已明显收口
    - 但 `token / semantic` 结果当前仍存在可见波动，尚不足以宣布彻底定版
    - 如果现在必须给版本验收结论，应表述为：
      - 当前完整批次闭环已成立
      - 题级失败已可归因
      - 但切分最终默认版本仍应保持“阶段性观察中”，不能再写成单边稳定领先
- 回归口径：
  - 能解释为什么当前不能再把 `token` 写成单边稳定领先
  - 能说明为什么当前还不宜宣布“彻底定版”

## 4. P1 任务池

### P1-1 公开应用能力最小验收闭环

- 挂载任务：
  - `#75`
- 当前状态：
  - 已确认存在：
    - `AppPublishConfigDO`
    - `AppPublishConfigService`
    - `AppPublishConfigServiceImpl`
    - `PublicAppService`
    - 访问侧 `Req / VO`
  - 本轮已补齐：
    - `AppController` 下的公开发布配置查询 / 保存 / 关闭入口
    - 前端公开发布配置 API
    - 工作台应用管理中的“公开发布”最小配置弹窗
    - `PublicAppController`
    - `PublicAppServiceImpl`
    - 前端公开访问页面 `/#/public/app/{slug}`
    - 前后端公开访问白名单
  - 本轮运行态回归已完成：
    - 本地 `backend` / `frontend` 编译构建通过
    - 本机 `localhost:9000` 重启到最新包后，匿名访问：
      - `GET /consumer/public/app/detail?slug=app-2067259775636033538`
      - 返回 `code=200`
    - 同一公开应用在 `PUBLIC` 模式下：
      - `POST /consumer/public/app/verifyPassword`
      - 返回 `当前公开应用无需访问密码`
    - 同一公开应用公开聊天链路已真实返回 SSE 内容，并带出参考来源
    - `2026-07-02` 又补了一轮公开访问留痕验证：
      - `POST /consumer/public/app/chat`
      - 访客 `visitorId=codex-verify-visitor`
      - SSE 正常返回
      - 调用记录 `2072582054155644930`
      - `invoke_record.username = will / 公开访客:codex-ve`
    - `2026-07-02 16:08` 又在本地独立 laughing 环境补了一轮可复跑最小验收：
      - 新增脚本：
        - `scripts/public-app-min-acceptance.sh`
      - 先修复了两个阻塞最小闭环的真实问题：
        - `ops/laughing/backend-prod-laughing.yml` 缺少 `/consumer/public/app/detail`
        - `/consumer/public/app/verifyPassword`
        - `/consumer/public/app/chat`
        - 导致公开访问接口在 laughing 环境里被 `AuthorizationInterceptor` 误拦截为 `权限不足`
        - `consumer/src/main/resources/mybatis/mapper/InvokeRecordMapper.xml`
        - `queryList` 子查询里未 `join app` 却直接使用 `app.app_name`
        - 导致按应用名回查调用记录时触发 `Unknown column 'app.app_name' in 'where clause'`
      - 修复后对本地应用 `RAG MVP 验收助手 #2071648764483952642`、slug `app-2071648764483952642` 重新验收：
        - `PUBLIC` 模式：
          - `GET /consumer/public/app/detail`
          - 返回 `code=200`
          - `passwordRequired=false`
          - `POST /consumer/public/app/verifyPassword`
          - 返回 `code=30000`
          - `msg=当前公开应用无需访问密码`
          - `POST /consumer/public/app/chat`
          - SSE 正常返回
          - 调用记录 `2072593191000461313`
          - `invoke_record.username = course_demo_user / 公开访客:codex-ve`
        - `PASSWORD` 模式：
          - `GET /consumer/public/app/detail`
          - 返回 `code=200`
          - `passwordRequired=true`
          - 错误密码返回：
            - `code=30000`
            - `msg=访问密码错误`
          - 正确密码返回：
            - `code=200`
            - `expireSeconds=43200`
          - 后续 `POST /consumer/public/app/chat`
          - SSE 正常返回
          - 调用记录 `2072593262710476802`
          - `invoke_record.username = course_demo_user / 公开访客:codex-ve`
      - 验收脚本执行后又把配置恢复回原始状态，未在本地验收库留下额外脏配置
- 当前判断：
  - 当前已不再只是“管理链路可用”
  - `#75` 的最小公开访问闭环已完成：
    - 可保存公开配置
    - 匿名可打开公开页
    - `PUBLIC / PASSWORD` 模式能按预期分流
    - 公开聊天链路不暴露 `appKey`
    - 访客请求已通过现有调用记录链路形成最小运行态留痕
  - 剩余未做完的更多是平台化增强项，不再属于当前 MVP 主完成度缺口
- 下一步动作：
  - 把本轮运行态证据回填到 ZenTao `#75`
  - 后续如继续推进，只再考虑平台化增强项：
    - 公开访问统计
    - 更完整的管理员治理
    - 自定义域名与更细的公开策略

### P1-2 依据展示与失败体面持续承接

- 挂载任务：
  - `#71`
  - `#72`
- 当前状态：
  - 已有承接位
  - 当前不再是主闭环最前排阻塞项
- 当前判断：
  - 更适合作为 `#73` 和 `#75` 的支持项继续留痕
- 下一步动作：
  - 跟随 `RQ-15` 和公开应用后续验收一起观察

## 5. 推荐执行顺序

如果只按“现在最影响产品完成度”的顺序推进，建议直接按下面执行：

1. `P0-2` 先把 `18` 条正式题库的完整批次可见性和部分落库闭环跑通
2. `P0-3` 围绕剩余候选题和真实业务问题继续扩题，不破坏正式基线
3. `P0-4` 基于更厚正式题库的稳定跑测结果，继续固化阶段性默认结论
4. `#71 / #72` 跟随 `#73` 的剩余问题继续留痕
5. `#74 / #75` 保持已闭环状态，后续仅按需做回归

## 6. 一句话结论

当前主 goal 不是缺少任务，而是缺少把现有证据持续推进成：

- `#73` 的单题收口与扩题动作
- `P0 / P1` 清单的稳定执行节奏
- `#75` 等后续能力的最小验收留痕

这份清单的作用，就是把这些事压成可直接执行的任务池。

## 7. 2026-07-01 当日新增推进

- `consumer` 已补一轮轻量工程改造，用于改善正式批次在厚样本池下的可观测性：
  - 批次开始时先创建记录
  - 每完成一题即落库一条 item
  - 批次摘要实时刷新 `已完成 x / 总数`
  - 中途中断时，摘要会保留“已完成数量 + 最近异常”
- 当前改造文件：
  - [consumer/src/main/java/com/osh/ai/assistant/consumer/service/impl/RagAcceptanceServiceImpl.java](/Users/tianyi/devproj/osh-qa-assistant/consumer/src/main/java/com/osh/ai/assistant/consumer/service/impl/RagAcceptanceServiceImpl.java:162)
- 编译验证：
  - `mvn -f pom.xml -pl consumer -am -DskipTests compile`
- 这轮改造的目标不是一次性做完整异步任务系统，而是先把“整批长时间不可见、无法判断是否已执行”的主痛点压掉。
- 本地 laughing 继续补到运行态实证：
  - 当前已经能看到两条新的 `18` 题正式批次完整落库
  - `semantic`：`2072237066467594242`，`18/18`
  - `token`：`2072238664249954306`，`18/18`
- 本轮还确认了另一个工程边界：
  - `publish experiment = 90000` 不一定是发布链路代码故障
  - 在本地直接覆盖 `.laughing-runtime/backend.jar` 但未彻底重建容器时，可能触发 jar 热替换后的类加载损坏
  - 典型异常：
    - `ZipException: too many length or distance symbols`
    - `NoClassDefFoundError: org/springframework/core/NestedCheckedException`
  - 容器显式 `restart` 后，同一最小 publish 请求已恢复 `code=200`
