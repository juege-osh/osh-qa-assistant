# RAG MVP 剩余关键闭环与任务池

## 1. 文档目的

这份文档用于收敛当前 `RAG MVP` 主 goal 的真实状态：

- 哪些关键闭环已经在当前代码与运行态中被证明成立
- 哪些事项仍然属于剩余任务
- 哪些事项虽然重要，但应进入 `P1 / P2`，而不是继续混入当前 MVP 主闭环

这份文档面向：

- 禅道任务整理
- 当前阶段排期判断
- 后续复盘时的状态对照

## 2. 本轮核验范围

本轮基于下面几类证据做核验：

- 产品基线文档
- 开发记录与验收文档
- 当前仓库实现
- laughing 本地运行态真实执行结果
- laughing QA 部署入口与远端只读可达性检查

关键证据包括：

- [文档/产品文档/RAG_MVP功能清单.md](/Users/tianyi/devproj/osh-qa-assistant/文档/产品文档/RAG_MVP功能清单.md)
- [文档/产品文档/RAG_MVP验收标准.md](/Users/tianyi/devproj/osh-qa-assistant/文档/产品文档/RAG_MVP验收标准.md)
- [文档/开发文档/2026-06-28_RAG_MVP测试问题集与效果记录.md](/Users/tianyi/devproj/osh-qa-assistant/文档/开发文档/2026-06-28_RAG_MVP测试问题集与效果记录.md)
- [scripts/rag-mvp-p0-audit.sh](/Users/tianyi/devproj/osh-qa-assistant/scripts/rag-mvp-p0-audit.sh)
- [scripts/laughing-alert-self-check.sh](/Users/tianyi/devproj/osh-qa-assistant/scripts/laughing-alert-self-check.sh)
- [ops/laughing/README.md](/Users/tianyi/devproj/osh-qa-assistant/ops/laughing/README.md)

## 3. 当前结论

### 3.1 已经完成并有证据支撑的闭环

当前可以确认已经闭环的事项：

- `RAG MVP` 真实问题集正式验收批次已可真实落库
- `token / semantic` 两个切分版本已可真实运行、复跑并留痕
- 补知识 -> 重建索引 -> 复跑正式验收 这条修复链路已打通
- 应用 `Prompt / 模型开关` 已可查询并纳入运行态核验
- 最小故障告警在 laughing 本地运行态已进入 `READY -> SENT` 可验证状态
- 真实问题集已从 6 条扩充到 10 条，并成功生成新一轮正式验收批次

### 3.2 本轮对“最小故障告警未闭环”的纠偏

此前文档里有一个阶段性结论：

- 剩余唯一未进入运行态已开启的 P0 是最小故障告警

但按当前仓库和运行态重新核验后，这个结论对于当前 laughing 运行态已经过时。

截至 `2026-07-01` 再次核验，本轮实际验证结果：

- `ENV_FILE=.laughing-runtime/.env bash scripts/rag-mvp-p0-audit.sh http://127.0.0.1:19088`
  - 正式验收批次：当前最新一次为 `FOLLOW_UP`
  - 跨切分版本对比：`PASS`
  - 生效切分版本：`PASS`
  - `Prompt / 模型开关`：`PASS`
  - 告警 `readiness`：`READY`
- `bash scripts/laughing-alert-self-check.sh http://127.0.0.1:19088`
  - `readiness=READY`
  - `selfCheck=SENT`

本轮还有一个很值得保留的环境现象：

- 在把最新 `backend.jar` 替换进 `.laughing-runtime` 并重建本地容器后，
  - `alertReadiness` 仍然是 `READY`
  - 但 `alertSelfCheck` 一度退化成 `系统异常`
- 后端日志明确定位到根因：
  - `qa-assistant-mailpit-laughing:1025` `UnknownHostException`
  - 也就是 mailpit 容器虽然还在运行，但不在当前 `laughing-runtime_qa-assistant-laughing-network` 网络里
- 在把 `qa-assistant-mailpit-laughing` 接回当前运行态网络后，再次验证：
  - `readiness=READY`
  - `selfCheck=SENT`

这说明当前 laughing 本地告警闭环仍然成立，但要特别注意：

- 这条闭环依赖 mailpit/SMTP 目标容器也处于同一运行态网络
- 如果只替换 `backend.jar`、重建后端容器，却没有同步检查 mailpit 所在网络，就会出现“readiness 正常、selfCheck 系统异常”的伪回归

因此这条经验应一并沉淀为本地运行态的环境操作注意事项，而不是误判成代码回退。

另外，远端 laughing QA 入口也已可达：

- `http://43.242.200.25:18088/ai/` 可正常打开前端页面
- `http://43.242.200.25:19088/consumer/ops/alertReadiness` 可访问到服务，只是未登录返回 `权限不足`

但进一步登录后核验发现，并且在 `2026-07-01` 再次复现确认：

- 共享 QA 环境调用 `GET /consumer/ops/alertReadiness` 返回 `code=90000, msg=系统异常`

因此应把这条结论收敛为：

- 最小故障告警不再属于当前已知未闭环项
- 当前已证明：
  - 代码链路存在
  - 运行态配置存在
  - 本地演练可达 `READY / SENT`
  - QA 入口服务在线
- 当前仍未补强的，是“共享 QA 环境登录后 `alertReadiness` 系统异常”的排查与留痕

这说明共享 QA 的剩余问题已经从“是否配置”升级成了“目标环境运行态异常排查”，但它仍然不影响“本地闭环已成立”这个结论。

进一步结合代码与部署配置核对，当前更具体的排查判断是：

- `alertReadiness` 本身只是读取 `AlertProperties` 并调用 `alertService.canSendAlertNow()`
- 代码里没有复杂业务逻辑，真正容易出问题的层主要集中在：
  - `AI_ASSISTANT_ALERT_*` 环境变量是否缺失、格式不对或绑定异常
  - `spring.mail.*` 与 `ai-assistant.alert.*` 的组合是否让 `JavaMailSender` 装配异常
  - `AI_ASSISTANT_ALERT_RECIPIENTS` / `AI_ASSISTANT_ALERT_SMTP_HOST` / `AI_ASSISTANT_ALERT_SELF_CHECK_ENABLED` 等关键键在 QA `.env` 中是否为空或写法不兼容
- 本机保存的 QA 部署源文件 [`.qaenv-laughing.env.local`](/Users/tianyi/devproj/osh-qa-assistant/.qaenv-laughing.env.local) 当前完全未包含任何 `AI_ASSISTANT_ALERT_*` 配置
- 如果共享 QA 确实按这份文件或同源配置部署，则说明告警链路至少不是“完整显式配置”状态
- 当前已尝试用本机现有 SSH key 只读连 QA 主机拉取 `/opt/qa-assistant-laughing/.env` 与容器日志，但被远端拒绝；因此下一步需要部署凭据或远端支持配合
- 本轮又直接执行了 [scripts/qaenv-laughing-remote-audit.sh](/Users/tianyi/devproj/osh-qa-assistant/scripts/qaenv-laughing-remote-audit.sh)，在当前默认连接参数下返回：
  - `Permission denied, please try again.`
  - `root@43.242.200.25: Permission denied (publickey,gssapi-keyex,gssapi-with-mic,password).`
- 这说明当前剩余阻塞可以写得更明确：
  - 不是“还没准备远端审计手段”
  - 而是“远端审计脚本已就绪，但当前缺少可用 QA 部署凭据或远端协助”

因此当前更推荐把 QA 告警问题按“部署配置/运行态绑定异常”优先排，而不是先怀疑接口实现本身。

本轮继续核对部署源、部署脚本与接口实现后，这个判断又可以再收紧一步：

- `scripts/qaenv-laughing-deploy.sh` 会把本机 `QAENV_RUNTIME_ENV_FILE`（默认就是 [`.qaenv-laughing.env.local`](/Users/tianyi/devproj/osh-qa-assistant/.qaenv-laughing.env.local)）原样 rsync 到远端 `$DEPLOY_DIR/.env`
- 当前这份本机 QA 部署源文件里：
  - 现已补齐最小故障告警所需 `AI_ASSISTANT_ALERT_*`
  - 并已补到共享 SMTP 使用 `465 + SSL` 时需要的 `AI_ASSISTANT_ALERT_SMTP_SSL_ENABLE / AI_ASSISTANT_ALERT_SMTP_SSL_TRUST`
- 与之对照：
  - [`.qaenv-laughing.env.example`](/Users/tianyi/devproj/osh-qa-assistant/.qaenv-laughing.env.example) 已经明确提供了完整的告警变量模板
  - 本地 laughing 运行态 [.laughing-runtime/.env](/Users/tianyi/devproj/osh-qa-assistant/.laughing-runtime/.env) 也是依赖这些变量才做到 `READY -> SENT`

结合代码逻辑，`GET /consumer/ops/alertReadiness` 的正常路径只会返回：

- `DISABLED`
- `SELF_CHECK_DISABLED`
- `CONFIG_MISSING`
- `SENDER_UNAVAILABLE`
- `READY`

它本身不应该因为“只是没填收件人/SMTP”就直接抛出 `90000`。这说明共享 QA 当前的 `code=90000, msg=系统异常` 更像是：

- 告警相关配置缺失后，又叠加了更早期的运行态绑定/装配异常
- 或共享 QA 远端真实 `.env` 与本机保存的部署源文件已经发生偏离

因此当前关于 QA 告警问题的优先级建议也应更明确：

- 不是先去改接口实现
- 而是先拿到共享 QA 当前真实 `.env` 和对应异常栈
- 同时对照本机部署源，确认是否漏配了整组 `AI_ASSISTANT_ALERT_*`
- 其中“先拿到”这一步当前已有真实阻塞证据：
  - `bash scripts/qaenv-laughing-remote-audit.sh` 已实际执行
  - 但当前机器默认可用的连接参数无法通过 SSH 登录 QA 主机
  - 因此下一步需要先补正确的部署凭据，或由远端同学协助导出 `.env`、docker 状态和日志摘要

本轮还补了一个更可执行的辅助证据：

- [scripts/qaenv-laughing-verify.sh](/Users/tianyi/devproj/osh-qa-assistant/scripts/qaenv-laughing-verify.sh)
  - 现在除了检查 QA Web 入口与 `auth/getCode` 联通性之外
  - 还会额外读取本机 `QAENV_RUNTIME_ENV_FILE`（默认就是 [`.qaenv-laughing.env.local`](/Users/tianyi/devproj/osh-qa-assistant/.qaenv-laughing.env.local)）
  - 并打印告警关键变量是否为 `set / missing`
  - 同时会输出：
    - `alertReadiness` 正常代码路径下理论只会出现的状态集合
    - 为什么当前 `code=90000` 更像远端真实 `.env` 漂移或额外运行态异常

当前实际执行结果已经明确显示：

- `AI_ASSISTANT_ALERT_ENABLED=missing`
- `AI_ASSISTANT_ALERT_SELF_CHECK_ENABLED=missing`
- `AI_ASSISTANT_ALERT_SELF_CHECK_ALLOWED_USERS=missing`
- `AI_ASSISTANT_ALERT_RECIPIENTS=missing`
- `AI_ASSISTANT_ALERT_FROM=missing`
- `AI_ASSISTANT_ALERT_SMTP_HOST=missing`
- `AI_ASSISTANT_ALERT_SMTP_PORT=missing`
- `AI_ASSISTANT_ALERT_SMTP_USERNAME=missing`

这意味着即使还没有远端 `.env` 实证，我们也已经可以确定：

- 当前本机保存的 QA 部署源不是“少一两个告警键”，而是“整组告警配置默认未纳入部署源”

同时，本轮继续把“为什么 `90000` 不是普通缺配应有返回”收紧成了更强判断：

- `ops/laughing/backend-prod-laughing.yml` 已明确支持：
  - `AI_ASSISTANT_ALERT_FROM`
  - `AI_ASSISTANT_ALERT_SELF_CHECK_ENABLED`
  - `AI_ASSISTANT_ALERT_SELF_CHECK_ALLOWED_USERS`
  - 以及整组 `AI_ASSISTANT_ALERT_*`
- `scripts/qaenv-laughing-deploy.sh` 会把本机 `.qaenv-laughing.env.local` 原样同步到远端 `.env`
- `OpsSelfCheckServiceImpl#queryAlertReadiness` 的正常返回状态只有：
  - `DISABLED`
  - `SELF_CHECK_DISABLED`
  - `CONFIG_MISSING`
  - `SENDER_UNAVAILABLE`
  - `READY`
- 因此如果共享 QA 仍未回到正常告警状态，当前更应优先怀疑远端 `.env`、旧包或更早期运行态异常，而不是本机部署源
- 当前真实出现的 `code=90000, msg=系统异常` 反而更像：
  - 远端真实 `.env` 与本机保存的部署源已发生偏离
  - 或远端运行态在属性绑定、`JavaMailSender` 装配、更早期 Bean 初始化处额外抛错

这意味着 `#74` 当前最关键的下一步已经更明确：

- 不是先改接口代码
- 而是先拿到共享 QA 当前真实 `.env` 和后端异常栈

### 3.3 扩题后的最新批次结论

本轮已把真实问题集扩充到 10 条，并先后复跑出两组正式验收批次。

第一组批次：

- `语义切分`：`7 / 10`
- `Token 切分`：`6 / 10`

这说明：

- 扩题是有效的，新的题集已经能拉出之前 6 题样本没暴露出来的缺口
- 当前不能简单沿用“Token 一定更优”这一旧结论
- 至少在这轮 10 题样本里，`semantic` 的整体通过数更高

本轮新暴露的典型缺口：

- `RQ-08`
  - 两个版本都没稳定答到“如何区分补知识 / 补切分 / 补提示词”
  - 说明这类“运营判断口径”知识仍然偏弱，更接近 `knowledge / prompt` 混合问题
- `RQ-10`
  - 两个版本都没稳定命中“切换生效切分版本后如何验证是否真的生效”
  - 说明“版本切换后的验证动作”知识表达仍然不够稳
- `RQ-06`
  - `Token` 版本被标记为 `chunking`
  - 说明长步骤型问题在 `Token` 版本下仍可能出现切散、遗漏或层次不稳

随后在补知识和补验收口径后，又跑出了第二组完整 `v2` 批次：

- `2026-07-01 真实问题集正式验收-语义切分-v2`：`8 / 10`
- `2026-07-01 真实问题集正式验收-Token切分-v2`：`7 / 10`

这组 `v2` 批次带来了两个重要变化：

- `RQ-08 / RQ-10` 已重新并入完整 10 题批次，并且两边都通过
- `RQ-06` 在完整批次里不再是命中缺口，只剩 `语义切分` 的 `readable=待确认`

按 `v2` 明细看，当前整批里真正还没全绿的点主要是：

- `RQ-09`
  - 两个版本都出现 `hit=待确认`
  - 但回答正文已经明确说明“当前知识库没有足够依据”并给出下一步建议
  - 更像是自动验收对“超出知识库范围”这类真实问法识别还不够稳
- `Token` 版本的 `RQ-03 / RQ-07`
  - 都是 `graceful=待确认`
  - 回答本身已成功命中且可读，剩余问题更接近“失败体面”口径偏保守，而不是明显答错
- `语义` 版本的 `RQ-06`
  - 当前是 `readable=待确认`
  - 更像长答案结构仍需观察，而不是当前主阻塞

但在同一轮代码口径继续调整并重新复跑完整 `v3` 批次后，结果再次发生反转：

- `2026-07-01 真实问题集正式验收-语义切分-v3`：`7 / 10`
- `2026-07-01 真实问题集正式验收-Token切分-v3`：`8 / 10`

这组 `v3` 批次说明当前真正的问题不是“哪一版已经稳稳胜出”，而是：

- 当前默认切分结论还不稳定
- 同一套 10 题样本在知识补丁、验收口径微调后，整体胜负还会发生反转
- 这证明“切分策略阶段性结论固化”仍然是主 goal 内的真实剩余项，不能下线

按 `v3` 明细看：

- `语义切分`
  - `RQ-06` 回到 `hit=待确认`，并重新出现 `followUpCategory=chunking`
  - `RQ-01 / RQ-08` 出现 `graceful=待确认`
- `Token切分`
  - `RQ-08 / RQ-09` 仍有 `graceful=待确认`
  - 但整体通过数已提升到 `8 / 10`

因此，当前阶段性默认结论应更新为：

- 现阶段没有证据证明 `semantic` 或 `Token` 已经稳定胜出
- `v2` 一度表现为 `semantic 8/10 > token 7/10`
- `v3` 又变成 `token 8/10 > semantic 7/10`
- 当前更合理的口径不是“直接定版”，而是：
  - 继续保留当前生效版本
  - 同时把 `RQ-06 / RQ-08 / RQ-09` 作为切分稳定性观察点
  - 再补一轮更厚的真实样本后重新判断默认方案

### 3.4 RQ-08 / RQ-10 的进一步验证结果

针对 `RQ-08 / RQ-10`，本轮继续做了两步推进：

1. 在补知识脚本里补充了：
   - 如何区分 `补知识 / 补切分 / 补提示词`
   - 切换生效切分版本后如何验证是否真的生效
2. 在 `RagAcceptanceServiceImpl` 里补充了这两类真实问题的命中判断口径

单独两题验证批次结果：

- 批次：`2026-06-30 单批验证-RQ08RQ10-口径修正`
- 结果：`2 / 2`

这说明当前更准确的判断是：

- `RQ-08 / RQ-10` 的瓶颈已经不再只是知识内容缺失
- 还有一部分是自动验收规则对真实业务问题的识别口径不够细
- 在补知识和补验收口径后，这两题已经可以稳定通过

因此这两题不再是“完全未解问题”，而且已经在完整 `v2` 批次中再次通过。当前更准确的状态是：

- 已在局部验证中通过
- 已重新并入完整 10 题正式验收批次并通过
- 后续只需要继续留在样本池里观察，不再属于当前最靠前的阻塞项

### 3.5 RQ-06 的进一步验证结果

针对 `RQ-06`，本轮补上了“按题号筛选复跑”的脚本能力，可以只对重点题做快速对比验证。

单题对比批次结果：

- `2026-07-01 真实问题集正式验收-RQ06-语义切分`：`1 / 1`
- `2026-07-01 真实问题集正式验收-RQ06-Token切分`：`1 / 1`

这说明当前更准确的判断是：

- `RQ-06` 并不是一个在当前版本下必然失败的稳定缺口
- 在最新代码、最新知识补丁和单题复跑条件下，`semantic / token` 都可以通过
- 因此 `RQ-06` 当前更像“需要在整批场景持续观察”的风险点，而不是已经锁死的当前阻塞项

这也意味着当前切分判断要更克制：

- 不能再只因为旧批次里 `RQ-06` 被标记为 `chunking`，就直接下结论说 `Token` 当前一定不适合
- 应该把 `RQ-06` 继续放回完整题集里，与其他题一起观察整体稳定性

### 3.6 RQ-01 / RQ-06 / RQ-08 / RQ-09 的关键题 `v5` 复核结果

针对 `v4` 里仍然摇摆的 4 个关键题，本轮继续做了两步修正：

1. 在 `RagAcceptanceServiceImpl` 中收紧“失败体面”判定，避免：
   - 覆盖范围题、运营判断题只是顺带提到“未命中时如何反馈”，就被误判为 `graceful=待确认`
2. 在顺序题判定里补充识别：
   - `第一步 / 第二步 / 第三步` 这类自然步骤表达，而不只识别 `1. 2. 3.`

修正后重新执行关键题批次：

- `2026-07-01 真实问题集正式验收-关键题-语义切分-v5`：`4 / 4`
- `2026-07-01 真实问题集正式验收-关键题-Token切分-v5`：`4 / 4`

对应批次号：

- `语义切分 v5`：`2072152705218854913`
- `Token切分 v5`：`2072152930150989826`

这组 `v5` 结果说明：

- `RQ-01 / RQ-08` 在 `v4` 中暴露的主要不是知识缺口，而是自动验收对“顺带提及失败场景”的误伤
- `RQ-06` 在 `Token v4` 中的 `hit=待确认`，主要也是顺序题判定口径偏死，不足以直接下结论说当前 `Token` 在这题上真实失败
- `RQ-09` 继续保持通过

因此当前更准确的状态应更新为：

- `RQ-01 / RQ-06 / RQ-08 / RQ-09` 不再属于当前最靠前的产品完成度缺口
- 这些题仍应继续保留在完整题集里做整批观察
- 但它们现在更适合作为“验收稳定性观察样例”，而不是继续作为当前主阻塞项

### 3.7 扩题后的完整 `v6` 结果与新增稳定性问题

本轮继续把真实问题集从 `10` 条扩到 `15` 条，新增：

- `RQ-11` 接手新训练测试任务前的最小检查项
- `RQ-12` 长步骤题 / 总结型题反复掉分时优先怀疑什么
- `RQ-13` 结论不够直接、收尾不够体面时优先修哪一层
- `RQ-14` 两个切分版本都答不稳且老提示“没有足够依据”时更像哪类问题
- `RQ-15` 最小可重复验收从开始到复跑后的动作顺序

扩题后首先成功落出的完整批次为：

- `2026-07-01 真实问题集正式验收-语义切分-v6`
  - 批次：`2072154382357446657`
  - 结果：`9 / 15`
  - `followUpCount=1`

从这组 `语义切分 v6` 明细看，新增 5 题是本轮最主要的新拉分点：

- `RQ-11`：`hit=不通过`
- `RQ-12`：`hit=不通过`
- `RQ-13`：`hit=待确认`
- `RQ-14`：`hit=不通过`
- `RQ-15`：`hit=不通过`，`grounded=待确认`

这说明当前真实问题集一旦向“运营判断口径”和“最小闭环动作顺序”扩厚，整批通过率会明显回落。

同时，本轮 `Token v6` 在首次完整批次对比时没有顺利形成可用结果，后台日志直接暴露出新的运行态问题：

- `MySQLTransactionRollbackException: Lock wait timeout exceeded; try restarting transaction`

进一步结合代码排查，当前已确认一个高风险实现点：

- `runBatch / runDefaultBatch` 原先整段都挂着事务
- 同一事务里既包含整批真实问答执行，也包含正式验收批次写库
- 这会把“长时间跑测 + 批次保存”包成一个长事务，显著放大锁等待风险

本轮已做的工程收敛动作：

- 把 `RagAcceptanceServiceImpl` 的事务边界收缩到 `saveBatch` 最终保存阶段
- `runBatch / runDefaultBatch` 不再用长事务包住整批执行过程

当前状态应谨慎表述为：

- 已完成一轮明确的事务边界收缩修复
- 修复后已成功补出完整批次：
  - `2026-07-01 真实问题集正式验收-Token切分-v6-retry`
  - 批次：`2072155556351524866`
  - 结果：`10 / 15`
  - `followUpCount=0`
- 当前至少已有一轮直接证据表明：
  - 事务收缩后，完整 `Token` 批次已经可以成功落库
  - 本轮未再次复现 `Lock wait timeout`

因此当前可更新的阶段性判断是：

- `P0-5` 已从“仅定位到问题”推进到“已完成一轮代码修复并拿到首轮成功批次验证”
- 但是否已经彻底稳定，仍建议至少再补一轮完整批次复跑作为二次确认

结合扩题后的两边最新完整批次，当前最新对比结果为：

- `语义切分 v6`：`9 / 15`
  - 批次：`2072154382357446657`
- `Token切分 v6-retry`：`10 / 15`
  - 批次：`2072155556351524866`

这意味着在扩题后的当前证据里：

- `token` 已经阶段性领先于 `semantic`
- 但新增的 `RQ-11 ~ RQ-15` 当时仍是两边共同拉分点，说明扩题一开始确实把“运营判断口径样本”和“最小闭环动作顺序”里的问题拉了出来

这说明当前完整批次对比的剩余问题已经不仅是“哪种切分更优”，还新增了一个工程稳定性问题：

- 当扩题后再次做完整批次写库时，`ragAcceptance.runBatch` 这条链路存在数据库锁等待超时风险

因此当前阶段性结论需要进一步收敛为：

- 真实问题样本池扩厚是有效的，而且已经开始拉出新缺口
- 当前默认切分结论仍不能收口，因为：
  - `语义 v6` 已拿到 `9 / 15`
  - `Token v6` 首次完整写库因数据库锁超时未形成可对比结果
- 当前主 goal 内新增了一个需要明确入池的工程问题：
  - 完整正式验收批次在扩题后存在数据库锁等待超时风险

### 3.8 新增题 `v7 / v8` 复核结果：主要误伤已收敛，当前最集中缺口收缩到 `RQ-15`

针对新增的 `RQ-11 ~ RQ-15`，本轮继续做了两类收敛动作：

1. 在 `RagAcceptanceServiceImpl` 中补充了这 5 类运营判断题的专门命中判定
2. 继续收紧“诊断类问题”的 `graceful` 误伤，避免因为回答里顺带提到“没有足够依据”，就把本来已经答对的运营判断题误判为失败反馈不体面

新增题筛选复跑结果如下。

第一轮新增题 `v7`：

- `2026-07-01 真实问题集正式验收-新增题-语义切分-v7`
  - 批次：`2072158020350885890`
  - 结果：`3 / 5`
- `2026-07-01 真实问题集正式验收-新增题-Token切分-v7`
  - 批次：`2072158206779310082`
  - 结果：`3 / 5`

`v7` 题级结果说明：

- 两边共同通过：`RQ-11 / RQ-12 / RQ-13`
- 两边共同剩余：`RQ-14 / RQ-15`
- 其中：
  - `RQ-14` 实际 `hit / grounded / readable` 都已经通过，只是 `graceful=待确认`
  - `RQ-15` 两边都还是 `hit=待确认`、`grounded=待确认`

因此 `v7` 的准确含义不是“新增 5 题有 2 题完全答不出来”，而是：

- `RQ-11 ~ RQ-13` 已基本证明前一轮掉分里有一部分是验收口径误伤
- `RQ-14` 更接近诊断类问题的 `graceful` 误伤
- `RQ-15` 才是新增题里最值得继续追的真实剩余项

随后在继续收紧诊断类 `graceful` 判定后，又复跑出新增题 `v8`：

- `2026-07-01 2026-07-01 真实问题集正式验收-新增题-语义切分-v8`
  - 批次：`2072160255440961537`
  - 结果：`4 / 5`
- `2026-07-01 2026-07-01 真实问题集正式验收-新增题-Token切分-v8`
  - 批次：`2072160468931035137`
  - 结果：`4 / 5`

按 `v8` 题级结果看：

- `语义切分 v8`
  - `RQ-11 / RQ-12 / RQ-13 / RQ-15`：全部通过
  - `RQ-14`：仅剩 `graceful=待确认`
- `Token切分 v8`
  - `RQ-11 / RQ-12 / RQ-13 / RQ-14`：全部通过
  - `RQ-15`：仍是 `hit=待确认`、`grounded=待确认`

这组 `v8` 结果带来两个关键更新：

- 新增题里的主要误伤已经明显收敛，不应再把 `RQ-11 ~ RQ-14` 一股脑视作“当前共同大缺口”
- 当前更准确的说法应改为：
  - `RQ-11 ~ RQ-13` 已基本收口
  - `RQ-14` 更像诊断题 `graceful` 口径仍需观察
  - `RQ-15` 才是当前新增题里最集中、最真实的剩余弱点

因此扩题后的后续重点也应同步收敛为：

- 继续保留新增题样本池
- 但不再把“新增 5 题整体失守”作为当前判断
- 重点转为继续收口 `RQ-15` 这类“最小可重复验收动作顺序”问题

### 3.9 完整 `v9` 结果：`token 14 / 15`，`semantic 13 / 15`，剩余问题已收敛到单题 `RQ-15`

在新增题 `v8` 收敛后，本轮又继续补出一轮完整 `15` 题跨版本复跑：

- `2026-07-01 2026-07-01 真实问题集正式验收-语义切分-v9`
  - 批次：`2072161540407934978`
  - 结果：`13 / 15`
  - `followUpCount=0`
- `2026-07-01 2026-07-01 真实问题集正式验收-Token切分-v9`
  - 批次：`2072162158577041410`
  - 结果：`14 / 15`
  - `followUpCount=0`

这组完整 `v9` 结果带来三条很重要的更新：

1. 事务边界收缩后的完整批次稳定性又多了一轮成功证据
   当前不再只有 `Token v6-retry` 一次完整成功，而是又补到一轮完整 `v9` 两边都成功落库。

2. 当前整批默认结论已经比 `v2 / v3` 时更稳
   最新整批证据为：
   - `semantic v9 = 13 / 15`
   - `token v9 = 14 / 15`
   这使得“当前阶段 `token` 更优”不再只是单轮偶然现象，而是和 `v6-retry 10/15 > 9/15` 形成了连续方向一致的证据。

3. 当前整批剩余问题已明显收缩
   `v9` 两边完整批次里唯一仍未通过的题，都是：
   - `RQ-15`
     - `hit=待确认`
     - `grounded=待确认`
     - `readable=通过`
     - `graceful=通过`

这意味着当前主 goal 下关于真实问题批次的最准确状态，已经可以收敛为：

- 新增运营判断题的大面积误伤已基本消化
- 完整正式验收批次也再次成功落库，数据库锁等待问题至少已拿到第二轮成功证据
- 当前最集中、最真实的产品剩余弱点，已经从“一批题都不稳”收缩到单题 `RQ-15`

因此当前阶段性默认结论也应同步更新为：

- 当前默认切分策略更推荐继续保留 `token`
- 原因不是“理论上更优”，而是最新完整证据里：
  - `Token v6-retry = 10 / 15 > Semantic v6 = 9 / 15`
  - `Token v9 = 14 / 15 > Semantic v9 = 13 / 15`
- 但这仍是“阶段性默认结论”，因为当前还没有把 `RQ-15` 彻底收口

### 3.10 新运行态下 `RQ-14 / RQ-15` 重点复跑：剩余缺口再次确认只剩 `RQ-15`

在把最新源码重新打包进 `backend.jar`、替换到 `.laughing-runtime` 并重建本地容器之后，本轮又专门对 `RQ-14 / RQ-15` 做了一次重点复跑：

- `2026-07-01 2026-07-01 真实问题集正式验收-RQ1415-语义切分-v10`
  - 批次：`2072195750211276802`
  - 结果：`1 / 2`
- `2026-07-01 2026-07-01 真实问题集正式验收-RQ1415-Token切分-v10`
  - 批次：`2072195861767180290`
  - 结果：`1 / 2`

题级结果完全一致：

- `RQ-14`
  - `pass=通过`
  - `hit=通过`
  - `grounded=通过`
  - `readable=通过`
  - `graceful=通过`
- `RQ-15`
  - `pass=待确认`
  - `hit=待确认`
  - `grounded=待确认`
  - `readable=通过`
  - `graceful=通过`

这次 `v10` 很重要，因为它说明：

- `RQ-14` 已经不再属于当前残留问题
- `RQ-15` 不是旧批次偶然现象，也不是旧运行态缓存问题
- 即使在最新源码 + 新容器运行态下，`RQ-15` 仍然是两种切分版本共同剩下的唯一重点缺口

因此当前关于内容侧剩余项的最准确结论可以进一步收敛为：

- 真实问题集当前最集中的内容缺口就是 `RQ-15`
- 它更像“最小可重复验收动作顺序”的知识表达/答案组织还不够稳定
- 这个问题应优先按 `知识表达 + 提示词组织` 的组合方向处理，而不是再优先怀疑切分

### 3.11 `RQ-15` 的进一步补知识与规则复核：内容已接近闭环，但最新单题结果受运行时噪声干扰

围绕 `RQ-15`，本轮又继续做了两步推进：

1. 在 `rag-mvp-knowledge-repair-run.sh` 中补充了更完整的 `RQ-15` 知识补丁
   - 明确“开始前必须先确认的 4 件事”
   - 明确“准备文档 -> 上传 -> 保存/发布 -> 重建索引 -> 跑测 -> 看批次 -> 决策修补 -> 复跑核对字段”的完整闭环
2. 在 `RagAcceptanceServiceImpl` 中补充了 `evaluateMinimalLoopQuestion(...)` 对数字步骤的识别
   - 从只识别 `第一步/第二步/先/然后/最后`
   - 扩展到也识别 `1. 2. 3. ... 10.` 以及 `再`

随后做了两轮验证：

- `RQ15-v11`
  - `语义切分`：`0 / 1`，批次 `2072196821361020929`
  - `Token切分`：`0 / 1`，批次 `2072196826213830657`
  - 但从回答正文看，两边都已经明显接近标准答案：
    - 语义版已包含“前置确认项 + 运行顺序 + 复跑核对字段”
    - Token版已包含更完整的 `1~12` 步闭环
  - 这说明旧规则对数字步骤识别不足，确实是当时的重要干扰项

- `RQ15-v12`
  - `语义切分`：`0 / 1`，批次 `2072197839047118850`
  - `Token切分`：本轮未形成可用结果
  - 但这次不能直接解读成“内容补丁无效”，因为同一轮日志已明确暴露出新的运行时噪声：
    - `previewSplit failed: 文件不存在`
    - 上游 `POST https://v2.aicodee.com/v1/chat/completions` 返回 `503 Service Unavailable`

因此当前对 `RQ-15` 的最准确状态应更新为：

- `RQ-15` 仍然是当前最前排内容缺口
- 但最新一轮单题 `0 / 1` 已混入运行时噪声，不能再当成纯内容失败证据
- 当前更合理的下一步是：
  - 先确保单题复跑过程不再夹带 `previewSplit` 文件缺失与上游 `503`
  - 再对 `RQ-15` 做一次干净复跑，确认“补知识 + 数字步骤识别修正”是否已经足以打穿

本轮继续追到一个更明确的更新：

- 在修正 `rag-mvp-knowledge-repair-run.sh` 里“同名补丁文件取第一个 file id”的问题后，
  - `RQ15-v13` 的日志里已经不再出现 `previewSplit failed: 文件不存在`
  - 说明这条本地脚本噪声已基本收敛
- 但同一轮 `RQ15-v13` 仍然失败，且失败原因已经明确变成：
  - 上游 `POST https://v2.aicodee.com/v1/chat/completions` 返回 `503 Service Unavailable`
  - 当前题目记录为：
    - `hit=不通过`
    - `grounded=不通过`
    - `followUpCategory=observe`
    - `failReason=Stream processing failed`
  - 最终回答退化为“当前知识库检索暂时不可用”的兜底话术

因此，当前关于 `RQ-15` 的最新最准确状态应再收敛一步：

- `RQ-15` 仍然是当前前排问题
- 但最新单题失败的主因已经从“脚本噪声”进一步收缩为“上游模型服务 503”
- 所以下一步如果要继续证明 `RQ-15` 是否已经被内容/规则修正打穿，应优先选择：
  - 等上游模型服务恢复后重试
  - 或在更稳定时段补一轮同题干净复跑

本轮又补到了一条更硬的新证据：

- 使用 `QUESTION_FILTER=RQ-15 bash scripts/rag-mvp-real-batch-compare.sh`
  - 在当前本地运行态下再次执行单题复跑
  - 语义切分批次：`2072210988651532290`
  - Token 切分批次：`2072211098059952129`
- 这次两边结果都已达到：
  - `1 / 1`
  - `hit=通过`
  - `grounded=通过`
  - `readable=通过`
  - `graceful=通过`
- 并且这轮没有再混入：
  - `previewSplit 文件不存在`
  - 上游 `chat/completions 503`

因此当前关于 `RQ-15` 的判断应进一步更新为：

- `RQ-15` 已不再是“当前一跑就必失败”的问题
- 在当前本地稳定运行态下，它已经具备被单题打穿的硬证据
- 当前剩余谨慎点已经从“内容是否成立”收缩为：
  - 是否还需要再补一轮整批正式批次，确认它在完整 15 题上下文里也稳定通过
  - 是否还需要观察不同时间窗口下会不会再被上游波动带回噪声

本轮随后又继续尝试补完整 `15` 题正式批次确认：

- 执行：
  - `REPORT_PATH=/tmp/rag-real-15-compare.md bash scripts/rag-mvp-real-batch-compare.sh`
- 但这轮没有形成可用报告文件，脚本最终超时退出：
  - `TimeoutError: timed out`
- 同时本地 backend 容器日志明确出现：
  - `java.lang.IllegalStateException: Stream processing failed`
  - `502 Bad Gateway from POST https://v2.aicodee.com/v1/chat/completions`

这说明当前最新状态应再补一层限定：

- `RQ-15` 单题本身已经有稳定窗口下的通过证据
- 但“把这条通过结论放回完整 15 题批次里再确认”这一步，当前仍会被上游模型服务稳定性打断
- 因此 `RQ-15` 当前最准确的状态不再是“内容未收口”，而是：
  - 内容侧已经明显收敛
  - 整批再次确认仍受上游 `502 / timeout` 影响

本轮继续回查批次列表后，还拿到了一个进一步修正判断：

- 上面那次完整 `15` 题复跑并不是“完全没有落库”
- 最新批次列表里已能看到语义侧新批次：
  - `2072214033196142594`
  - `2026-07-01 真实问题集正式验收-语义切分`
  - `itemCount=15`
  - `passCount=11`
  - `followUpCount=4`
- 这条批次里：
  - `RQ-15` 已在整批上下文里通过
  - 当前未通过的 4 题反而变成：
    - `RQ-06`
    - `RQ-07`
    - `RQ-08`
    - `RQ-09`
  - 但这 4 题的共同特征并不是内容方向错误，而是：
    - `followUpCategory=observe`
    - `failReason=Stream processing failed`
    - 回答统一退化为“当前知识库检索暂时不可用”

因此当前更准确的最新结论应更新为：

- `RQ-15` 已经不只是在单题里通过，而是在最新完整语义批次里也通过了
- 当前完整批次没有继续扩大内容缺口，最新掉分点主要是上游模型服务波动带来的运行态失败
- 也就是说，当前前排问题的性质已经从“内容未收口”继续向“运行态稳定性不足”偏移

## 4. 当前主 goal 下真正剩余的事项

围绕“完成 RAG MVP 剩余关键闭环，并形成可执行任务池”，当前真正剩余的内容主要有 3 类。

### 4.1 剩余项 A：扩充真实业务问题集

当前已有：

- 默认问题集自动跑测
- 真实问题集自动跑测
- 按当前生效版本复跑
- 跨切分版本对比
- 补知识复跑

当前仍不足的地方：

- 虽然已经扩到 15 条，但样本覆盖仍不算厚
- 问题类型还不够覆盖长期运营场景
- 当前任务池更多来自少量批次，而不是持续沉淀

这项仍是当前主 goal 内的重点，因为它直接影响：

- 命中问题
- 可信
- 易懂
- 失败体面

### 4.2 剩余项 B：切分策略阶段性结论固化

当前证据显示：

- 旧 6 题样本里，`token` 版本曾表现更稳
- 第一轮 10 题样本里，`semantic=7/10`，`token=6/10`
- 第二轮完整 `v2` 样本里，`semantic=8/10`，`token=7/10`
- 最新完整 `v3` 样本里，`semantic=7/10`，`token=8/10`
- 当前生效实验版本为 `脚本语义切分版`

当前仍未完全收口的不是“能不能对比”，而是：

- 哪个版本更适合作为当前默认实践
- 在什么题型下优先怀疑 `chunking`
- 哪些问题应优先走补知识而不是切分参数调整

也就是说，这条链路已经“能跑”，但还需要形成更明确的运营决策口径。

当前更具体的阶段性判断：

- `RQ-06` 在旧整批结果里暴露过 `chunking`，但在最新单题对比里两边都能通过，因此当前更适合视为“持续观察项”
- `RQ-08 / RQ-10` 这类运营判断题，已经在完整 `v2` 批次中通过，不再属于当前前排缺口
- `RQ-09` 这类“超出知识库范围”的真实问法，一度暴露出自动验收口径偏保守，现已补到单题 `1 / 1`
- `RQ-01 / RQ-06 / RQ-08 / RQ-09` 在关键题 `v5` 批次中已全部达到 `4 / 4`
- 扩题后的 `语义 v6` 已回落到 `9 / 15`，新增的 `RQ-11 ~ RQ-15` 已经开始暴露运营判断口径缺口
- 扩题后的 `Token v6-retry` 已达到 `10 / 15`，当前已阶段性高于 `语义 v6`
- 但最新 `v3` 整批又说明：
  - `RQ-06` 在 `semantic` 版本下仍可能回到 `chunking`
  - `RQ-08 / RQ-09` 在 `Token` 版本下仍可能留下 `graceful=待确认`
  - 所以当前更像“整体稳定性还在波动”，而不是已经可以收口

### 4.3 剩余项 C：把现有问题持续沉淀为任务池

当前前端已经具备：

- 待跟进分类
- `补知识 / 补切分 / 补提示词 / 补展示 / 补观测`
- 任务池草稿导出
- `P0 / P1 / P2` 优先级建议

见：

- [frontend/ai-assistant-web/src/views/personal/invokerecord/InvokeRecordManage.vue](/Users/tianyi/devproj/osh-qa-assistant/frontend/ai-assistant-web/src/views/personal/invokerecord/InvokeRecordManage.vue)

但当前任务池沉淀还不够稳定，主要原因不是工具缺失，而是：

- 真正长期保留的问题样本还不够多
- 当前更多是批次级闭环，较少形成持续维护的任务清单

## 5. 当前不应继续混入主 goal 的事项

### 5.1 公开应用能力

公开应用相关实现已经不是纯方案状态，当前仓库里已有：

- `AppPublishConfigDO`
- `AppPublishAccessTypeEnum`
- `AppPublishConfigService`
- `AppPublishConfigServiceImpl`
- `PublicAppService`
- `AppPublishSaveReq`
- `AppPublishConfigVO`
- `PublicAppChatReq`
- `PublicAppVerifyPasswordReq`
- `PublicAppDetailVO`
- `PublicAppAccessVO`
- `AppPublishConfigMapper`

但本轮核验时也确认：

- 已有“公开配置保存 / 查询”这一层的数据模型、入参和服务能力
- 已有面向公开访问链路的 `PublicAppService` 接口与访问侧 `Req / VO`
- 但当前仓库里仍未检索到：
  - 明确的公开应用控制器入口
  - `PublicAppServiceImpl`
  - 前端公开访问页面入口
- 没有看到像 `RAG MVP` 一样完整的运行态验收留痕

因此这块更适合定义为：

- `P1：公开应用能力验收闭环`

而不是继续并入当前 `RAG MVP` 主闭环。

### 5.2 更完整的平台化能力

下面这些事项仍然未完成，但按产品基线本来就不应优先进入当前 MVP 主闭环：

- 完整版本管理 UI
- 多维回滚
- 复杂公开应用治理
- 更重的管理员治理能力
- 更完整的平台化扩展能力

这些应保留在后续 `P1 / P2` 路线，不作为当前主 goal 的完成阻塞。

## 6. 当前任务池建议

### 6.0 当前可直接落 ZenTao 的说明

本轮已对 ZenTao 做只读核对，并确认：

- 当前机器可通过已有 token 访问 ZenTao API
- 执行 `2`（`6月8-6月14 主网站bug接龙及需求`）下面已经存在一组 `RAG MVP` 相关任务
- 当前最直接相关的现有任务包括：
  - `#74 RAG MVP：共享 QA 环境最小故障告警 alertReadiness=90000 排查与留痕`
  - `#75 公开应用能力：补一轮最小验收闭环并判断是否进入下一阶段`
  - `#71 RAG-1-未命中知识与异常场景的体面反馈`
  - `#72 RAG-2-聊天回答的依据与参考来源展示`
  - `#73 RAG-3-MVP测试问题集与效果记录闭环`

因此当前更准确的推进方式不是“完全从零新建任务池”，而是：

- 先复用并更新现有 `#71 / #72 / #73`
- 再把当前新暴露出来、但 ZenTao 里还没有明确承接项的 `P0 / P1` 缺口补成新任务

截至 `2026-07-01` 本轮又进一步形成了真实进展：

- 已通过本地脚本把执行记录真实追加回：
  - `#73 RAG-3-MVP测试问题集与效果记录闭环`
  - `#71 RAG-1-未命中知识与异常场景的体面反馈`
- 已在 ZenTao 里真实新建：
  - `#74 RAG MVP：共享 QA 环境最小故障告警 alertReadiness=90000 排查与留痕`
  - `#75 公开应用能力：补一轮最小验收闭环并判断是否进入下一阶段`
- 这说明当前主 goal 在 ZenTao 里已经不只是“可映射”，而是已经开始形成真实留痕

## 任务 1：扩充真实业务问题集并形成持续回归样本池

- 建议优先级：`P0`
- 目标：把当前真实问题集从“可跑”提升到“足够代表真实场景”
- 与现有 ZenTao 任务关系：
  - 可优先挂到 `#73 RAG-3-MVP测试问题集与效果记录闭环`
- 当前最新状态：
  - 共享题库文件已落地：`scripts/rag-mvp-real-questions.json`
  - 当前题数：`15`
  - 已成功跑出 15 题正式验收批次
  - 最新完整批次已达到：
    - `semantic v9`：`13 / 15`
    - `token v9`：`14 / 15`
- 产出物：
  - 继续新增一轮更完整的真实问题集
  - 至少覆盖高频知识问答、任务指导、模糊问题、未命中知识、高风险外部问题
  - 新一轮正式验收批次留痕
- 验收方式：
  - 新批次成功落库
  - 可从调用记录或正式验收页直接查看结果
  - 至少产出一份新任务池草稿
- 建议 ZenTao 标题：
  - `RAG MVP：扩充真实业务问题集并沉淀持续回归样本池`
- 建议 ZenTao 完成口径：
  - 新增一轮真实业务问题样本
  - 覆盖高频知识问答、任务指导、模糊问题、未命中知识、高风险外部问题
  - 至少形成一组新的正式验收批次与问题清单留痕

## 任务 2：固化当前切分策略阶段性默认结论

- 建议优先级：`P0`
- 目标：形成当前默认切分策略的阶段性运营口径
- 与现有 ZenTao 任务关系：
  - 可先作为 `#73` 的执行记录/子目标推进
  - 如果后续需要单独跟踪 `RQ-15 / token vs semantic`，再独立拆新任务
- 当前最新状态：
  - 第一轮 10 题：`semantic 7 / 10`，`token 6 / 10`
  - 完整 `v2`：`semantic 8 / 10`，`token 7 / 10`
  - 最新完整 `v3`：`semantic 7 / 10`，`token 8 / 10`
  - 扩题后的完整 `v9`：`semantic 13 / 15`，`token 14 / 15`
  - 当前扩题后的整批差距已经收缩到单题 `RQ-15`
  - 但 `RQ-15` 的最新单题失败又混入了上游 `chat/completions 503` 噪声，暂时不能直接当成纯切分失败
- 产出物：
  - 当前默认生效版本建议
  - 哪些问题优先归因 `chunking`
  - 哪些问题优先走补知识
  - 至少一轮对比复跑证据
- 验收方式：
  - 当前知识库生效版本与对比批次可对应
  - 文档中能明确写出当前默认建议，而不是只保留原始批次结果
- 建议 ZenTao 标题：
  - `RAG MVP：固化 semantic / token 切分阶段性默认结论`
- 建议 ZenTao 完成口径：
  - 基于扩题后的正式验收批次输出当前默认建议
  - 明确 `RQ-06 / RQ-08 / RQ-09` 等观察点的归因方向
  - 保留至少一轮完整跨版本复跑证据

## 任务 3：把真实问题批次持续沉淀为可执行任务池

- 建议优先级：`P0`
- 目标：让“跑测 -> 分类 -> 进入任务池 -> 修复 -> 复跑”成为稳定动作
- 与现有 ZenTao 任务关系：
  - 当前最适合继续挂到 `#73`
- 当前最值得先入池的问题：
  - 不再建议把 `RQ-01 / RQ-06 / RQ-08 / RQ-09` 直接当作当前前排产品缺口入池
- 更合理的入池口径应改为：
  - 整批 `semantic / token` 默认方案仍会反转
  - 真实问题样本池仍然偏薄
  - QA 告警目标环境仍未完成异常定位
  - 扩题后的完整正式验收批次存在数据库锁等待超时风险
- 产出物：
  - 一份可直接贴禅道的任务池草稿
  - 至少一轮按 `followUpCategory` 分类后的明确任务
  - 每个任务都有对应回归样例
- 验收方式：
  - 任务池不是泛总结，而是带具体问题、建议动作、回归口径
- 建议 ZenTao 标题：
  - `RAG MVP：把真实问题批次沉淀为 P0/P1 可执行任务池`
- 建议 ZenTao 完成口径：
  - 输出一份按 `P0 / P1` 分层的任务池草稿
  - 每个任务包含具体样例、建议动作、回归口径
  - 可直接转贴到 ZenTao 或周计划

## 任务 4：补共享 QA 环境告警验收留痕

- 建议优先级：`P0`
- 目标：把“共享 QA 环境登录后系统异常”定位到明确原因并留痕
- 与现有 ZenTao 任务关系：
  - 当前未看到可直接承接该问题的现成任务
  - 建议按下面标题新建独立任务
- 当前状态：
  - QA 前端入口可达
  - QA 后端告警接口可达
  - 未登录访问返回 `权限不足`
  - `2026-07-01` 再次确认：登录后访问 `alertReadiness` 返回 `code=90000, msg=系统异常`
  - 结合代码与部署脚本核对，当前最可疑层为 QA `.env` 中的 `AI_ASSISTANT_ALERT_*` 配置与 Spring 绑定
  - 本机保存的 QA 部署源文件当前未显式提供任何 `AI_ASSISTANT_ALERT_*` 配置
  - 使用本机现有 SSH key 无法直接只读拉取 QA 远端 `.env` 与日志，需改用正确部署凭据或请远端协助
- 验收方式：
  - 先读取 QA 实际 `.env` 或等价配置，逐项核对：
    - `AI_ASSISTANT_ALERT_ENABLED`
    - `AI_ASSISTANT_ALERT_SELF_CHECK_ENABLED`
    - `AI_ASSISTANT_ALERT_RECIPIENTS`
    - `AI_ASSISTANT_ALERT_SMTP_HOST`
    - `AI_ASSISTANT_ALERT_SMTP_PORT`
    - `AI_ASSISTANT_ALERT_SMTP_USERNAME`
    - `AI_ASSISTANT_ALERT_FROM`
  - 再结合 QA 后端日志定位 `90000` 的真实异常栈
  - 修复后重新读取 `alertReadiness`
  - 如条件允许，再补一次 `alertSelfCheck`
  - 记录修复前后状态、日志与结果
- 建议 ZenTao 标题：
  - `RAG MVP：共享 QA 环境最小故障告警 alertReadiness=90000 排查与留痕`
- 建议 ZenTao 完成口径：
  - 明确 `90000` 根因
  - 修复后登录态 `alertReadiness` 可返回非异常结果
  - 如条件允许补一次 `alertSelfCheck`
  - 留下修复前后结果和日志证据

## 任务 5：公开应用能力进入单独验收闭环

- 建议优先级：`P1`
- 目标：确认公开应用能力到底是“半成品能力”还是“可交付能力”
- 与现有 ZenTao 任务关系：
  - 当前未看到直接对应的现成任务
  - 建议保留为后续独立 `P1`
- 当前判断：
  - 已具备公开配置保存 / 查询这一层的实体、枚举、入参与服务实现
  - 已具备公开访问侧的接口定义和访问 `Req / VO`
  - 但当前仓库里仍未检索到明确的公开控制器入口、`PublicAppServiceImpl` 与前端公开页面入口
  - 运行态访问留痕仍为空
- 验收方式：
  - 是否存在完整接口入口
  - 是否存在前端页面入口
  - 是否存在真实访问留痕
  - 是否能给出第一版取舍结论
- 建议 ZenTao 标题：
  - `公开应用能力：补一轮最小验收闭环并判断是否进入下一阶段`
- 建议 ZenTao 完成口径：
  - 补齐接口、页面、访问留痕三类证据
  - 形成“可交付 / 半成品 / 延后”的结论

## 7. 当前建议排期

建议按下面顺序推进：

1. 并行排查共享 QA 环境告警 `90000` 异常
2. 再扩一轮真实业务问题集，避免继续只靠 10 题样本反复拉扯默认结论
3. 优先把完整正式验收批次的数据库锁等待超时问题收敛清楚
4. 再用扩题后的问题集稳定复跑 `semantic / token`
5. 再根据新批次结果决定当前默认切分建议
6. 把整批结果沉淀为持续任务池
7. 公开应用能力单独开 `P1` 验收任务

原因：

- 前四项最直接影响当前 `RAG MVP` 是否真的“可持续验收、可持续优化”
- 告警主能力在本地已闭环，但共享 QA 环境 `90000` 现在应视为目标环境 `P0` 运行态问题
- 公开应用能力重要，但不应继续稀释当前主 goal

## 8. 当前完成口径建议

如果要判断当前主 goal 是否完成，建议至少同时满足：

- 真实业务问题集有一轮扩充后的正式验收留痕
- 当前切分策略有阶段性默认结论，并能给出对应复跑证据
- 任务池已经整理为明确的 `P0 / P1` 清单
- 告警项至少补到“本地已完成 + QA 异常已定位或已修复”的明确状态

在这之前，不建议把当前主 goal 直接判定为完成。
