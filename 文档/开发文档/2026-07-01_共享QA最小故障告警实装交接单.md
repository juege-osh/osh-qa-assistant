# 2026-07-01 共享 QA 最小故障告警实装交接单

## 1. 当前结论

- 本地部署源已就绪，最小故障告警所需 `AI_ASSISTANT_ALERT_*` 配置已补齐。
- 本地构建产物已就绪，`backend/target/backend.jar` 与 `.laughing-runtime/backend.jar` 哈希一致。
- 共享 QA 对外基础入口仍可访问：
  - `http://43.242.200.25:18088/ai/`
  - `http://43.242.200.25:19088/auth/getCode`
- `2026-07-01 19:19` 已对共享 QA laughing 执行真实重建部署。
- 部署后远端 `.env` 已带齐整组 `AI_ASSISTANT_ALERT_*`，旧的 `No static resource consumer/ops/alertReadiness` 日志不再出现。
- 共享 QA 登录态已真实回归通过：
  - `alertReadiness=READY`
  - `alertSelfCheck=SENT`
- `#74` 当前已从“待排障”推进到“目标环境已实装并完成最小回归”。

## 2. 本地已完成证据

- 部署源校验：
  - `bash scripts/qaenv-laughing-remediation-plan.sh`
  - 结论：`readyInSource=true`
- 本地最小验证：
  - `bash scripts/qaenv-laughing-verify.sh`
  - 结论：`alert_profile_status=configured`
- 本地 jar 一致性：
  - `backend/target/backend.jar`
  - `.laughing-runtime/backend.jar`
  - 当前 sha256 一致

## 3. 当前远端已知事实

- 历史远端现象已经明确：
  - 远端 `/opt/qa-assistant-laughing/.env` 缺整组 `AI_ASSISTANT_ALERT_*`
  - 远端日志命中 `No static resource consumer/ops/alertReadiness`
  - 远端 `/opt/qa-assistant-laughing/backend.jar` 是 `2026-06-25` 旧包
- 因此当前更像：
  - 远端 `.env` 漂移
  - 远端 jar 未更新
  - 或更新 jar 后未重建容器
- 本轮修复后已确认：
  - 远端 `qa-assistant-backend-laughing` 与 `qa-assistant-nginx-laughing` 容器已于 `2026-07-01 19:19` 重建
  - 远端 `.env` 中 `AI_ASSISTANT_ALERT_ENABLED`、`AI_ASSISTANT_ALERT_SELF_CHECK_ENABLED`、`AI_ASSISTANT_ALERT_RECIPIENTS`、`AI_ASSISTANT_ALERT_SMTP_*` 均为 `set`
  - 远端审计输出里 `backendLogHits=[]`
  - `http://43.242.200.25:19088/auth/getCode` 已恢复 `code=200`

## 4. 拿到凭据后的执行顺序

### 4.1 先做远端审计

优先执行：

```bash
bash scripts/qaenv-laughing-remote-audit.sh
```

支持两种凭据方式：

- SSH key 已可登录：

```bash
QAENV_DEPLOY_USER=root \
QAENV_DEPLOY_HOST=43.242.200.25 \
QAENV_DEPLOY_PORT=58753 \
bash scripts/qaenv-laughing-remote-audit.sh
```

- 只有密码：

```bash
QAENV_DEPLOY_PASSWORD='***' \
bash scripts/qaenv-laughing-remote-audit.sh
```

确认点：

- 远端 `.env` 中 `AI_ASSISTANT_ALERT_*` 是否齐全
- 远端容器是否为 `qa-assistant-backend-laughing`
- 后端日志是否仍出现 `No static resource consumer/ops/alertReadiness`
- 是否还有 `90000 / 系统异常 / MailException / JavaMailSender` 等异常线索

### 4.2 再做部署

如果远端审计确认仍是旧包或旧 `.env`，执行：

```bash
bash scripts/qaenv-laughing-deploy.sh
```

同样支持两种凭据方式：

- SSH key 已可登录：

```bash
QAENV_DEPLOY_USER=root \
QAENV_DEPLOY_HOST=43.242.200.25 \
QAENV_DEPLOY_PORT=58753 \
bash scripts/qaenv-laughing-deploy.sh
```

- 只有密码：

```bash
QAENV_DEPLOY_PASSWORD='***' \
bash scripts/qaenv-laughing-deploy.sh
```

该脚本会完成：

- 构建最新 `backend.jar`
- 构建前端 `dist`
- 上传 `backend.jar`
- 上传 `docker-compose.yml`
- 上传 `backend-prod-laughing.yml`
- 上传 `nginx/default.conf`
- 上传远端 `.env`
- 执行 `docker compose up -d --remove-orphans --force-recreate`

## 5. 部署后回归检查

### 5.1 远端审计复跑

```bash
bash scripts/qaenv-laughing-remote-audit.sh
```

本轮实际结果：

- 远端 `.env` 不再缺 `AI_ASSISTANT_ALERT_*`
- 不再出现 `No static resource consumer/ops/alertReadiness`
- `qa-assistant-backend-laughing`、`qa-assistant-nginx-laughing` 已重建并重新启动

### 5.2 接口回归

登录态下回归：

- `/consumer/ops/alertReadiness`
- `/consumer/ops/alertSelfCheck`

`alertReadiness` 的可接受结果应收敛到以下之一，而不是 `404/90000`：

- `DISABLED`
- `SELF_CHECK_DISABLED`
- `CONFIG_MISSING`
- `SENDER_UNAVAILABLE`
- `READY`

本轮实际结果：

- `alertReadiness=READY`
- `alertSelfCheck=SENT`

## 6. 风险提醒

- 直接热替换 `backend.jar` 但不重建容器，可能出现假故障：
  - `ZipException`
  - `NoClassDefFoundError: org/springframework/core/NestedCheckedException`
  - `publish experiment = 90000`
- 所以必须以容器重建后的结果为准，不能只看 jar 文件是否上传成功。

## 7. 当前推荐结论

- `#74` 已完成目标环境实装与最小运行态回归，不再是当前主 goal 的第一阻塞项。
- 后续更合适的定位是：
  1. 把这轮结果回填到 ZenTao `#74`
  2. 保留 `qaenv-laughing-remote-audit.sh` 和 `qaenv-laughing-deploy.sh` 作为后续回归与漂移排查入口
  3. 将当前主推进重心切回 `#73` 的正式题库稳定批次、样本池扩厚与切分收口
