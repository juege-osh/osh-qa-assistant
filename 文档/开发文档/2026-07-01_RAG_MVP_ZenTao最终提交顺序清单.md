# RAG MVP ZenTao 最终提交顺序清单

## 1. 文档目的

这份清单只回答一个问题：

- 如果现在就要把当前 `RAG MVP` 剩余闭环落到 ZenTao，推荐按什么顺序提交

目标是降低执行歧义，避免：

- 先建了不该先建的任务
- 把 `#71 / #72 / #73` 和新任务混着提
- 重复创建已经有承接位的任务

## 2. 提交前先做什么

先跑：

```bash
bash scripts/zentao-rag-audit.sh
```

当前应重点关注脚本输出里的这几块：

- `directGoalTasks`
  - 这是当前主 goal 的直接承接任务
  - 当前应看到：
    - `#71`
    - `#72`
    - `#73`
    - `#74`
    - `#75`
- `keywordOnlyMatches`
  - 这是关键词撞上的噪声任务
  - 当前不要把它们混入这轮主 goal 提交
- `suggestedNewTasks`
  - 当前这项应为空；如果又出现待新建项，说明任务承接位与现状有漂移

## 3. 推荐提交顺序

### 第 1 步：先回填 `#73`

原因：

- 当前最核心的扩题、切分对比、任务池沉淀都已经能挂到 `#73`
- 这一步最能体现“主 goal 已经持续推进”

建议回填内容：

- 真实问题集正式题库已扩到 `18` 条
- 本地 `18` 题正式批次已能完整落库：
  - `semantic = 18/18`
  - `token = 18/18`
- 当前阶段性判断：
  - 更厚正式题库下“整批完全不可见”的问题已明显缓解
  - 但切分默认结论与候选题池 `RQ-16 / RQ-20` 仍未彻底收口

直接可复制正文：

- 见 [2026-07-01_RAG_MVP_ZenTao执行正文模板.md](/Users/tianyi/devproj/osh-qa-assistant/文档/开发文档/2026-07-01_RAG_MVP_ZenTao执行正文模板.md) 的 `3.1`

如果要用脚本安全追加到任务描述，建议流程：

```bash
cat >/tmp/zentao-note-73.md <<'EOF'
2026-07-01 执行记录

- 已把真实问题集正式题库扩充到 18 条，题库文件：scripts/rag-mvp-real-questions.json
- 本地 laughing 已查询到两条新的 18 题正式批次：
  - 语义切分：2072237066467594242，18/18
  - Token切分：2072238664249954306，18/18
- 当前阶段性判断：
  - 更厚正式题库下“整批完全不可见”的问题在本地新包下已明显缓解
  - 但当前仍未建议直接把切分方案彻底定版，候选题池 `RQ-16 / RQ-20` 与完整批次持续证据仍需继续观察
EOF

TASK_ID=73 NOTE_FILE=/tmp/zentao-note-73.md bash scripts/zentao-task-append-note.sh
```

当前状态：

- 这一步已于 `2026-07-01` 真实执行并回填成功

### 第 2 步：推进并回填 `#74`

原因：

- 这是当前最影响产品完成度的目标环境缺口
- 任务承接位已经存在，但还没完成环境实装闭环
- 当前已经有远端只读审计结果，可以直接进入修复准备而不是继续停留在猜测阶段

建议优先回填内容：

- 远端 `.env` 缺整组 `AI_ASSISTANT_ALERT_*`
- 远端日志命中 `No static resource consumer/ops/alertReadiness`
- 远端 `backend.jar` 仍是 `2026-06-25` 旧包，不是本地当前验证包
- 本机 QA 部署源 `.qaenv-laughing.env.local` 已补齐最小故障告警配置，并通过本地校验
- 共享 SMTP 当前使用 `465 + SSL`，laughing deploy profile 已补上 SMTP SSL 映射

当前状态：

- `#74` 已于 `2026-07-01` 真实创建成功
- 下一步不再是“继续查有没有问题”，而是等待共享 QA 可用 SSH 凭据后执行真实重建部署

### 第 3 步：继续把样本池和切分阶段性结论挂回 `#73`

原因：

- 当前 `#73` 的核心已经从单题收口，转成“18 题正式池是否稳定、样本池是否够厚、切分默认结论是否能留住证据”
- 所以更适合继续集中回填到一个任务里，而不是分裂成多个散任务

建议继续回填的内容：

- 当前正式池已是 `18` 题，候选池只剩 `RQ-16 / RQ-20`
- `token` 仍可保留为阶段性默认候选，但暂不对外宣布“彻底定版”
- 下一步重点是继续拉厚真实业务样本，并保留完整批次复跑证据

### 第 4 步：如有必要，再补 `#71 / #72`

适用场景：

- 你希望把“失败体面”和“依据展示”也同步留痕
- 或者这两项正在被单独关注

否则可以先不动，避免这轮提交太散。

直接可复制正文：

- `#71` 见 [2026-07-01_RAG_MVP_ZenTao执行正文模板.md](/Users/tianyi/devproj/osh-qa-assistant/文档/开发文档/2026-07-01_RAG_MVP_ZenTao执行正文模板.md) 的 `3.2`
- `#72` 见 [2026-07-01_RAG_MVP_ZenTao执行正文模板.md](/Users/tianyi/devproj/osh-qa-assistant/文档/开发文档/2026-07-01_RAG_MVP_ZenTao执行正文模板.md) 的 `3.3`

当前状态：

- `#71` 已于 `2026-07-01` 真实回填成功
- `#72` 当前仍未回填，不是主阻塞，可后置

### 第 5 步：最后推进并回填 `#75`

原因：

- 这块重要，但不属于当前主闭环最前排阻塞
- 更适合在 `#74` 和 `#73` 再推进一轮之后补“接口 / 页面 / 访问留痕”三类证据

当前状态：

- `#75` 已于 `2026-07-01` 真实创建成功
- 后续重点就是补齐“接口 / 页面 / 访问留痕”三类证据，而不是再新建任务

## 4. 一句话顺序

如果只记最短版本，当前推荐顺序就是：

1. 先回填 `#73`
2. 再推进并回填 `#74`
3. 再继续把样本池 / `token vs semantic` 证据回挂 `#73`
4. 有余力再补 `#71 / #72`
5. 最后推进并回填 `#75`

## 5. 当前不建议怎么做

当前不建议：

- 先建一堆新任务，再回头看 `#73`
- 把 `keywordOnlyMatches` 里的任务误当作当前主 goal 承接项
- 在候选题池 `RQ-16 / RQ-20` 还没收口、完整批次证据还在持续补齐前，就把切分方案彻底定版
- 在 QA 告警远端实证还没拿到前，就把 `P0-1` 判完成
